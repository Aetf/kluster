IMAGE=golinks

# Upstream publishes neither releases nor tags, so the source is pinned to a
# commit and bumped by hand (`git ls-remote https://github.com/kellegous/go
# HEAD`); there is deliberately no renovate hint here, because there is no
# version stream for renovate to follow. TAG is <commit date>-<short sha>.
GOLINKS_COMMIT=df1275169beb561f80aa8936b8f297cecdcea10f

TAG=20260821-df12751
