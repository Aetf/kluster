# coding=utf-8
# *** WARNING: this file was generated by crd2pulumi. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from ... import _utilities
from . import outputs
from ... import meta as _meta

__all__ = [
    'XBackendTrafficPolicy',
    'XBackendTrafficPolicySpec',
    'XBackendTrafficPolicySpecPatch',
    'XBackendTrafficPolicySpecRetryConstraint',
    'XBackendTrafficPolicySpecRetryConstraintBudget',
    'XBackendTrafficPolicySpecRetryConstraintBudgetPatch',
    'XBackendTrafficPolicySpecRetryConstraintMinRetryRate',
    'XBackendTrafficPolicySpecRetryConstraintMinRetryRatePatch',
    'XBackendTrafficPolicySpecRetryConstraintPatch',
    'XBackendTrafficPolicySpecSessionPersistence',
    'XBackendTrafficPolicySpecSessionPersistenceCookieConfig',
    'XBackendTrafficPolicySpecSessionPersistenceCookieConfigPatch',
    'XBackendTrafficPolicySpecSessionPersistencePatch',
    'XBackendTrafficPolicySpecTargetRefs',
    'XBackendTrafficPolicySpecTargetRefsPatch',
    'XBackendTrafficPolicyStatus',
    'XBackendTrafficPolicyStatusAncestors',
    'XBackendTrafficPolicyStatusAncestorsAncestorRef',
    'XBackendTrafficPolicyStatusAncestorsAncestorRefPatch',
    'XBackendTrafficPolicyStatusAncestorsConditions',
    'XBackendTrafficPolicyStatusAncestorsConditionsPatch',
    'XBackendTrafficPolicyStatusAncestorsPatch',
    'XBackendTrafficPolicyStatusPatch',
    'XListenerSet',
    'XListenerSetSpec',
    'XListenerSetSpecListeners',
    'XListenerSetSpecListenersAllowedRoutes',
    'XListenerSetSpecListenersAllowedRoutesKinds',
    'XListenerSetSpecListenersAllowedRoutesKindsPatch',
    'XListenerSetSpecListenersAllowedRoutesNamespaces',
    'XListenerSetSpecListenersAllowedRoutesNamespacesPatch',
    'XListenerSetSpecListenersAllowedRoutesNamespacesSelector',
    'XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressions',
    'XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressionsPatch',
    'XListenerSetSpecListenersAllowedRoutesNamespacesSelectorPatch',
    'XListenerSetSpecListenersAllowedRoutesPatch',
    'XListenerSetSpecListenersPatch',
    'XListenerSetSpecListenersTls',
    'XListenerSetSpecListenersTlsCertificateRefs',
    'XListenerSetSpecListenersTlsCertificateRefsPatch',
    'XListenerSetSpecListenersTlsPatch',
    'XListenerSetSpecParentRef',
    'XListenerSetSpecParentRefPatch',
    'XListenerSetSpecPatch',
    'XListenerSetStatus',
    'XListenerSetStatusConditions',
    'XListenerSetStatusConditionsPatch',
    'XListenerSetStatusListeners',
    'XListenerSetStatusListenersConditions',
    'XListenerSetStatusListenersConditionsPatch',
    'XListenerSetStatusListenersPatch',
    'XListenerSetStatusListenersSupportedKinds',
    'XListenerSetStatusListenersSupportedKindsPatch',
    'XListenerSetStatusPatch',
    'XMesh',
    'XMeshSpec',
    'XMeshSpecParametersRef',
    'XMeshSpecParametersRefPatch',
    'XMeshSpecPatch',
    'XMeshStatus',
    'XMeshStatusConditions',
    'XMeshStatusConditionsPatch',
    'XMeshStatusPatch',
    'XMeshStatusSupportedFeatures',
    'XMeshStatusSupportedFeaturesPatch',
]

@pulumi.output_type
class XBackendTrafficPolicy(dict):
    """
    XBackendTrafficPolicy defines the configuration for how traffic to a
    target backend should be handled.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "apiVersion":
            suggest = "api_version"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicy. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicy.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicy.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 api_version: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 metadata: Optional['_meta.v1.outputs.ObjectMeta'] = None,
                 spec: Optional['outputs.XBackendTrafficPolicySpec'] = None,
                 status: Optional['outputs.XBackendTrafficPolicyStatus'] = None):
        """
        XBackendTrafficPolicy defines the configuration for how traffic to a
        target backend should be handled.
        :param _builtins.str api_version: APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        :param _builtins.str kind: Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        :param '_meta.v1.ObjectMetaArgs' metadata: Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
        """
        if api_version is not None:
            pulumi.set(__self__, "api_version", 'gateway.networking.x-k8s.io/v1alpha1')
        if kind is not None:
            pulumi.set(__self__, "kind", 'XBackendTrafficPolicy')
        if metadata is not None:
            pulumi.set(__self__, "metadata", metadata)
        if spec is not None:
            pulumi.set(__self__, "spec", spec)
        if status is not None:
            pulumi.set(__self__, "status", status)

    @_builtins.property
    @pulumi.getter(name="apiVersion")
    def api_version(self) -> Optional[_builtins.str]:
        """
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        """
        return pulumi.get(self, "api_version")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def metadata(self) -> Optional['_meta.v1.outputs.ObjectMeta']:
        """
        Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
        """
        return pulumi.get(self, "metadata")

    @_builtins.property
    @pulumi.getter
    def spec(self) -> Optional['outputs.XBackendTrafficPolicySpec']:
        return pulumi.get(self, "spec")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional['outputs.XBackendTrafficPolicyStatus']:
        return pulumi.get(self, "status")


@pulumi.output_type
class XBackendTrafficPolicySpec(dict):
    """
    Spec defines the desired state of BackendTrafficPolicy.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "retryConstraint":
            suggest = "retry_constraint"
        elif key == "sessionPersistence":
            suggest = "session_persistence"
        elif key == "targetRefs":
            suggest = "target_refs"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicySpec. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicySpec.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicySpec.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 retry_constraint: Optional['outputs.XBackendTrafficPolicySpecRetryConstraint'] = None,
                 session_persistence: Optional['outputs.XBackendTrafficPolicySpecSessionPersistence'] = None,
                 target_refs: Optional[Sequence['outputs.XBackendTrafficPolicySpecTargetRefs']] = None):
        """
        Spec defines the desired state of BackendTrafficPolicy.
        :param Sequence['XBackendTrafficPolicySpecTargetRefsArgs'] target_refs: TargetRefs identifies API object(s) to apply this policy to.
               Currently, Backends (A grouping of like endpoints such as Service,
               ServiceImport, or any implementation-specific backendRef) are the only
               valid API target references.
               
               Currently, a TargetRef can not be scoped to a specific port on a
               Service.
        """
        if retry_constraint is not None:
            pulumi.set(__self__, "retry_constraint", retry_constraint)
        if session_persistence is not None:
            pulumi.set(__self__, "session_persistence", session_persistence)
        if target_refs is not None:
            pulumi.set(__self__, "target_refs", target_refs)

    @_builtins.property
    @pulumi.getter(name="retryConstraint")
    def retry_constraint(self) -> Optional['outputs.XBackendTrafficPolicySpecRetryConstraint']:
        return pulumi.get(self, "retry_constraint")

    @_builtins.property
    @pulumi.getter(name="sessionPersistence")
    def session_persistence(self) -> Optional['outputs.XBackendTrafficPolicySpecSessionPersistence']:
        return pulumi.get(self, "session_persistence")

    @_builtins.property
    @pulumi.getter(name="targetRefs")
    def target_refs(self) -> Optional[Sequence['outputs.XBackendTrafficPolicySpecTargetRefs']]:
        """
        TargetRefs identifies API object(s) to apply this policy to.
        Currently, Backends (A grouping of like endpoints such as Service,
        ServiceImport, or any implementation-specific backendRef) are the only
        valid API target references.

        Currently, a TargetRef can not be scoped to a specific port on a
        Service.
        """
        return pulumi.get(self, "target_refs")


@pulumi.output_type
class XBackendTrafficPolicySpecPatch(dict):
    """
    Spec defines the desired state of BackendTrafficPolicy.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "retryConstraint":
            suggest = "retry_constraint"
        elif key == "sessionPersistence":
            suggest = "session_persistence"
        elif key == "targetRefs":
            suggest = "target_refs"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicySpecPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicySpecPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicySpecPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 retry_constraint: Optional['outputs.XBackendTrafficPolicySpecRetryConstraintPatch'] = None,
                 session_persistence: Optional['outputs.XBackendTrafficPolicySpecSessionPersistencePatch'] = None,
                 target_refs: Optional[Sequence['outputs.XBackendTrafficPolicySpecTargetRefsPatch']] = None):
        """
        Spec defines the desired state of BackendTrafficPolicy.
        :param Sequence['XBackendTrafficPolicySpecTargetRefsPatchArgs'] target_refs: TargetRefs identifies API object(s) to apply this policy to.
               Currently, Backends (A grouping of like endpoints such as Service,
               ServiceImport, or any implementation-specific backendRef) are the only
               valid API target references.
               
               Currently, a TargetRef can not be scoped to a specific port on a
               Service.
        """
        if retry_constraint is not None:
            pulumi.set(__self__, "retry_constraint", retry_constraint)
        if session_persistence is not None:
            pulumi.set(__self__, "session_persistence", session_persistence)
        if target_refs is not None:
            pulumi.set(__self__, "target_refs", target_refs)

    @_builtins.property
    @pulumi.getter(name="retryConstraint")
    def retry_constraint(self) -> Optional['outputs.XBackendTrafficPolicySpecRetryConstraintPatch']:
        return pulumi.get(self, "retry_constraint")

    @_builtins.property
    @pulumi.getter(name="sessionPersistence")
    def session_persistence(self) -> Optional['outputs.XBackendTrafficPolicySpecSessionPersistencePatch']:
        return pulumi.get(self, "session_persistence")

    @_builtins.property
    @pulumi.getter(name="targetRefs")
    def target_refs(self) -> Optional[Sequence['outputs.XBackendTrafficPolicySpecTargetRefsPatch']]:
        """
        TargetRefs identifies API object(s) to apply this policy to.
        Currently, Backends (A grouping of like endpoints such as Service,
        ServiceImport, or any implementation-specific backendRef) are the only
        valid API target references.

        Currently, a TargetRef can not be scoped to a specific port on a
        Service.
        """
        return pulumi.get(self, "target_refs")


@pulumi.output_type
class XBackendTrafficPolicySpecRetryConstraint(dict):
    """
    RetryConstraint defines the configuration for when to allow or prevent
    further retries to a target backend, by dynamically calculating a 'retry
    budget'. This budget is calculated based on the percentage of incoming
    traffic composed of retries over a given time interval. Once the budget
    is exceeded, additional retries will be rejected.

    For example, if the retry budget interval is 10 seconds, there have been
    1000 active requests in the past 10 seconds, and the allowed percentage
    of requests that can be retried is 20% (the default), then 200 of those
    requests may be composed of retries. Active requests will only be
    considered for the duration of the interval when calculating the retry
    budget. Retrying the same original request multiple times within the
    retry budget interval will lead to each retry being counted towards
    calculating the budget.

    Configuring a RetryConstraint in BackendTrafficPolicy is compatible with
    HTTPRoute Retry settings for each HTTPRouteRule that targets the same
    backend. While the HTTPRouteRule Retry stanza can specify whether a
    request will be retried, and the number of retry attempts each client
    may perform, RetryConstraint helps prevent cascading failures such as
    retry storms during periods of consistent failures.

    After the retry budget has been exceeded, additional retries to the
    backend MUST return a 503 response to the client.

    Additional configurations for defining a constraint on retries MAY be
    defined in the future.

    Support: Extended
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "minRetryRate":
            suggest = "min_retry_rate"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicySpecRetryConstraint. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicySpecRetryConstraint.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicySpecRetryConstraint.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 budget: Optional['outputs.XBackendTrafficPolicySpecRetryConstraintBudget'] = None,
                 min_retry_rate: Optional['outputs.XBackendTrafficPolicySpecRetryConstraintMinRetryRate'] = None):
        """
        RetryConstraint defines the configuration for when to allow or prevent
        further retries to a target backend, by dynamically calculating a 'retry
        budget'. This budget is calculated based on the percentage of incoming
        traffic composed of retries over a given time interval. Once the budget
        is exceeded, additional retries will be rejected.

        For example, if the retry budget interval is 10 seconds, there have been
        1000 active requests in the past 10 seconds, and the allowed percentage
        of requests that can be retried is 20% (the default), then 200 of those
        requests may be composed of retries. Active requests will only be
        considered for the duration of the interval when calculating the retry
        budget. Retrying the same original request multiple times within the
        retry budget interval will lead to each retry being counted towards
        calculating the budget.

        Configuring a RetryConstraint in BackendTrafficPolicy is compatible with
        HTTPRoute Retry settings for each HTTPRouteRule that targets the same
        backend. While the HTTPRouteRule Retry stanza can specify whether a
        request will be retried, and the number of retry attempts each client
        may perform, RetryConstraint helps prevent cascading failures such as
        retry storms during periods of consistent failures.

        After the retry budget has been exceeded, additional retries to the
        backend MUST return a 503 response to the client.

        Additional configurations for defining a constraint on retries MAY be
        defined in the future.

        Support: Extended
        """
        if budget is not None:
            pulumi.set(__self__, "budget", budget)
        if min_retry_rate is not None:
            pulumi.set(__self__, "min_retry_rate", min_retry_rate)

    @_builtins.property
    @pulumi.getter
    def budget(self) -> Optional['outputs.XBackendTrafficPolicySpecRetryConstraintBudget']:
        return pulumi.get(self, "budget")

    @_builtins.property
    @pulumi.getter(name="minRetryRate")
    def min_retry_rate(self) -> Optional['outputs.XBackendTrafficPolicySpecRetryConstraintMinRetryRate']:
        return pulumi.get(self, "min_retry_rate")


@pulumi.output_type
class XBackendTrafficPolicySpecRetryConstraintBudget(dict):
    """
    Budget holds the details of the retry budget configuration.
    """
    def __init__(__self__, *,
                 interval: Optional[_builtins.str] = None,
                 percent: Optional[_builtins.int] = None):
        """
        Budget holds the details of the retry budget configuration.
        :param _builtins.str interval: Interval defines the duration in which requests will be considered
               for calculating the budget for retries.
               
               Support: Extended
        :param _builtins.int percent: Percent defines the maximum percentage of active requests that may
               be made up of retries.
               
               Support: Extended
        """
        if interval is not None:
            pulumi.set(__self__, "interval", interval)
        if percent is not None:
            pulumi.set(__self__, "percent", percent)

    @_builtins.property
    @pulumi.getter
    def interval(self) -> Optional[_builtins.str]:
        """
        Interval defines the duration in which requests will be considered
        for calculating the budget for retries.

        Support: Extended
        """
        return pulumi.get(self, "interval")

    @_builtins.property
    @pulumi.getter
    def percent(self) -> Optional[_builtins.int]:
        """
        Percent defines the maximum percentage of active requests that may
        be made up of retries.

        Support: Extended
        """
        return pulumi.get(self, "percent")


@pulumi.output_type
class XBackendTrafficPolicySpecRetryConstraintBudgetPatch(dict):
    """
    Budget holds the details of the retry budget configuration.
    """
    def __init__(__self__, *,
                 interval: Optional[_builtins.str] = None,
                 percent: Optional[_builtins.int] = None):
        """
        Budget holds the details of the retry budget configuration.
        :param _builtins.str interval: Interval defines the duration in which requests will be considered
               for calculating the budget for retries.
               
               Support: Extended
        :param _builtins.int percent: Percent defines the maximum percentage of active requests that may
               be made up of retries.
               
               Support: Extended
        """
        if interval is not None:
            pulumi.set(__self__, "interval", interval)
        if percent is not None:
            pulumi.set(__self__, "percent", percent)

    @_builtins.property
    @pulumi.getter
    def interval(self) -> Optional[_builtins.str]:
        """
        Interval defines the duration in which requests will be considered
        for calculating the budget for retries.

        Support: Extended
        """
        return pulumi.get(self, "interval")

    @_builtins.property
    @pulumi.getter
    def percent(self) -> Optional[_builtins.int]:
        """
        Percent defines the maximum percentage of active requests that may
        be made up of retries.

        Support: Extended
        """
        return pulumi.get(self, "percent")


@pulumi.output_type
class XBackendTrafficPolicySpecRetryConstraintMinRetryRate(dict):
    """
    MinRetryRate defines the minimum rate of retries that will be allowable
    over a specified duration of time.

    The effective overall minimum rate of retries targeting the backend
    service may be much higher, as there can be any number of clients which
    are applying this setting locally.

    This ensures that requests can still be retried during periods of low
    traffic, where the budget for retries may be calculated as a very low
    value.

    Support: Extended
    """
    def __init__(__self__, *,
                 count: Optional[_builtins.int] = None,
                 interval: Optional[_builtins.str] = None):
        """
        MinRetryRate defines the minimum rate of retries that will be allowable
        over a specified duration of time.

        The effective overall minimum rate of retries targeting the backend
        service may be much higher, as there can be any number of clients which
        are applying this setting locally.

        This ensures that requests can still be retried during periods of low
        traffic, where the budget for retries may be calculated as a very low
        value.

        Support: Extended
        :param _builtins.int count: Count specifies the number of requests per time interval.
               
               Support: Extended
        :param _builtins.str interval: Interval specifies the divisor of the rate of requests, the amount of
               time during which the given count of requests occur.
               
               Support: Extended
        """
        if count is not None:
            pulumi.set(__self__, "count", count)
        if interval is not None:
            pulumi.set(__self__, "interval", interval)

    @_builtins.property
    @pulumi.getter
    def count(self) -> Optional[_builtins.int]:
        """
        Count specifies the number of requests per time interval.

        Support: Extended
        """
        return pulumi.get(self, "count")

    @_builtins.property
    @pulumi.getter
    def interval(self) -> Optional[_builtins.str]:
        """
        Interval specifies the divisor of the rate of requests, the amount of
        time during which the given count of requests occur.

        Support: Extended
        """
        return pulumi.get(self, "interval")


@pulumi.output_type
class XBackendTrafficPolicySpecRetryConstraintMinRetryRatePatch(dict):
    """
    MinRetryRate defines the minimum rate of retries that will be allowable
    over a specified duration of time.

    The effective overall minimum rate of retries targeting the backend
    service may be much higher, as there can be any number of clients which
    are applying this setting locally.

    This ensures that requests can still be retried during periods of low
    traffic, where the budget for retries may be calculated as a very low
    value.

    Support: Extended
    """
    def __init__(__self__, *,
                 count: Optional[_builtins.int] = None,
                 interval: Optional[_builtins.str] = None):
        """
        MinRetryRate defines the minimum rate of retries that will be allowable
        over a specified duration of time.

        The effective overall minimum rate of retries targeting the backend
        service may be much higher, as there can be any number of clients which
        are applying this setting locally.

        This ensures that requests can still be retried during periods of low
        traffic, where the budget for retries may be calculated as a very low
        value.

        Support: Extended
        :param _builtins.int count: Count specifies the number of requests per time interval.
               
               Support: Extended
        :param _builtins.str interval: Interval specifies the divisor of the rate of requests, the amount of
               time during which the given count of requests occur.
               
               Support: Extended
        """
        if count is not None:
            pulumi.set(__self__, "count", count)
        if interval is not None:
            pulumi.set(__self__, "interval", interval)

    @_builtins.property
    @pulumi.getter
    def count(self) -> Optional[_builtins.int]:
        """
        Count specifies the number of requests per time interval.

        Support: Extended
        """
        return pulumi.get(self, "count")

    @_builtins.property
    @pulumi.getter
    def interval(self) -> Optional[_builtins.str]:
        """
        Interval specifies the divisor of the rate of requests, the amount of
        time during which the given count of requests occur.

        Support: Extended
        """
        return pulumi.get(self, "interval")


@pulumi.output_type
class XBackendTrafficPolicySpecRetryConstraintPatch(dict):
    """
    RetryConstraint defines the configuration for when to allow or prevent
    further retries to a target backend, by dynamically calculating a 'retry
    budget'. This budget is calculated based on the percentage of incoming
    traffic composed of retries over a given time interval. Once the budget
    is exceeded, additional retries will be rejected.

    For example, if the retry budget interval is 10 seconds, there have been
    1000 active requests in the past 10 seconds, and the allowed percentage
    of requests that can be retried is 20% (the default), then 200 of those
    requests may be composed of retries. Active requests will only be
    considered for the duration of the interval when calculating the retry
    budget. Retrying the same original request multiple times within the
    retry budget interval will lead to each retry being counted towards
    calculating the budget.

    Configuring a RetryConstraint in BackendTrafficPolicy is compatible with
    HTTPRoute Retry settings for each HTTPRouteRule that targets the same
    backend. While the HTTPRouteRule Retry stanza can specify whether a
    request will be retried, and the number of retry attempts each client
    may perform, RetryConstraint helps prevent cascading failures such as
    retry storms during periods of consistent failures.

    After the retry budget has been exceeded, additional retries to the
    backend MUST return a 503 response to the client.

    Additional configurations for defining a constraint on retries MAY be
    defined in the future.

    Support: Extended
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "minRetryRate":
            suggest = "min_retry_rate"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicySpecRetryConstraintPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicySpecRetryConstraintPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicySpecRetryConstraintPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 budget: Optional['outputs.XBackendTrafficPolicySpecRetryConstraintBudgetPatch'] = None,
                 min_retry_rate: Optional['outputs.XBackendTrafficPolicySpecRetryConstraintMinRetryRatePatch'] = None):
        """
        RetryConstraint defines the configuration for when to allow or prevent
        further retries to a target backend, by dynamically calculating a 'retry
        budget'. This budget is calculated based on the percentage of incoming
        traffic composed of retries over a given time interval. Once the budget
        is exceeded, additional retries will be rejected.

        For example, if the retry budget interval is 10 seconds, there have been
        1000 active requests in the past 10 seconds, and the allowed percentage
        of requests that can be retried is 20% (the default), then 200 of those
        requests may be composed of retries. Active requests will only be
        considered for the duration of the interval when calculating the retry
        budget. Retrying the same original request multiple times within the
        retry budget interval will lead to each retry being counted towards
        calculating the budget.

        Configuring a RetryConstraint in BackendTrafficPolicy is compatible with
        HTTPRoute Retry settings for each HTTPRouteRule that targets the same
        backend. While the HTTPRouteRule Retry stanza can specify whether a
        request will be retried, and the number of retry attempts each client
        may perform, RetryConstraint helps prevent cascading failures such as
        retry storms during periods of consistent failures.

        After the retry budget has been exceeded, additional retries to the
        backend MUST return a 503 response to the client.

        Additional configurations for defining a constraint on retries MAY be
        defined in the future.

        Support: Extended
        """
        if budget is not None:
            pulumi.set(__self__, "budget", budget)
        if min_retry_rate is not None:
            pulumi.set(__self__, "min_retry_rate", min_retry_rate)

    @_builtins.property
    @pulumi.getter
    def budget(self) -> Optional['outputs.XBackendTrafficPolicySpecRetryConstraintBudgetPatch']:
        return pulumi.get(self, "budget")

    @_builtins.property
    @pulumi.getter(name="minRetryRate")
    def min_retry_rate(self) -> Optional['outputs.XBackendTrafficPolicySpecRetryConstraintMinRetryRatePatch']:
        return pulumi.get(self, "min_retry_rate")


@pulumi.output_type
class XBackendTrafficPolicySpecSessionPersistence(dict):
    """
    SessionPersistence defines and configures session persistence
    for the backend.

    Support: Extended
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "absoluteTimeout":
            suggest = "absolute_timeout"
        elif key == "cookieConfig":
            suggest = "cookie_config"
        elif key == "idleTimeout":
            suggest = "idle_timeout"
        elif key == "sessionName":
            suggest = "session_name"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicySpecSessionPersistence. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicySpecSessionPersistence.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicySpecSessionPersistence.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 absolute_timeout: Optional[_builtins.str] = None,
                 cookie_config: Optional['outputs.XBackendTrafficPolicySpecSessionPersistenceCookieConfig'] = None,
                 idle_timeout: Optional[_builtins.str] = None,
                 session_name: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        SessionPersistence defines and configures session persistence
        for the backend.

        Support: Extended
        :param _builtins.str absolute_timeout: AbsoluteTimeout defines the absolute timeout of the persistent
               session. Once the AbsoluteTimeout duration has elapsed, the
               session becomes invalid.
               
               Support: Extended
        :param _builtins.str idle_timeout: IdleTimeout defines the idle timeout of the persistent session.
               Once the session has been idle for more than the specified
               IdleTimeout duration, the session becomes invalid.
               
               Support: Extended
        :param _builtins.str session_name: SessionName defines the name of the persistent session token
               which may be reflected in the cookie or the header. Users
               should avoid reusing session names to prevent unintended
               consequences, such as rejection or unpredictable behavior.
               
               Support: Implementation-specific
        :param _builtins.str type: Type defines the type of session persistence such as through
               the use a header or cookie. Defaults to cookie based session
               persistence.
               
               Support: Core for "Cookie" type
               
               Support: Extended for "Header" type
        """
        if absolute_timeout is not None:
            pulumi.set(__self__, "absolute_timeout", absolute_timeout)
        if cookie_config is not None:
            pulumi.set(__self__, "cookie_config", cookie_config)
        if idle_timeout is not None:
            pulumi.set(__self__, "idle_timeout", idle_timeout)
        if session_name is not None:
            pulumi.set(__self__, "session_name", session_name)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="absoluteTimeout")
    def absolute_timeout(self) -> Optional[_builtins.str]:
        """
        AbsoluteTimeout defines the absolute timeout of the persistent
        session. Once the AbsoluteTimeout duration has elapsed, the
        session becomes invalid.

        Support: Extended
        """
        return pulumi.get(self, "absolute_timeout")

    @_builtins.property
    @pulumi.getter(name="cookieConfig")
    def cookie_config(self) -> Optional['outputs.XBackendTrafficPolicySpecSessionPersistenceCookieConfig']:
        return pulumi.get(self, "cookie_config")

    @_builtins.property
    @pulumi.getter(name="idleTimeout")
    def idle_timeout(self) -> Optional[_builtins.str]:
        """
        IdleTimeout defines the idle timeout of the persistent session.
        Once the session has been idle for more than the specified
        IdleTimeout duration, the session becomes invalid.

        Support: Extended
        """
        return pulumi.get(self, "idle_timeout")

    @_builtins.property
    @pulumi.getter(name="sessionName")
    def session_name(self) -> Optional[_builtins.str]:
        """
        SessionName defines the name of the persistent session token
        which may be reflected in the cookie or the header. Users
        should avoid reusing session names to prevent unintended
        consequences, such as rejection or unpredictable behavior.

        Support: Implementation-specific
        """
        return pulumi.get(self, "session_name")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        Type defines the type of session persistence such as through
        the use a header or cookie. Defaults to cookie based session
        persistence.

        Support: Core for "Cookie" type

        Support: Extended for "Header" type
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XBackendTrafficPolicySpecSessionPersistenceCookieConfig(dict):
    """
    CookieConfig provides configuration settings that are specific
    to cookie-based session persistence.

    Support: Core
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lifetimeType":
            suggest = "lifetime_type"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicySpecSessionPersistenceCookieConfig. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicySpecSessionPersistenceCookieConfig.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicySpecSessionPersistenceCookieConfig.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 lifetime_type: Optional[_builtins.str] = None):
        """
        CookieConfig provides configuration settings that are specific
        to cookie-based session persistence.

        Support: Core
        :param _builtins.str lifetime_type: LifetimeType specifies whether the cookie has a permanent or
               session-based lifetime. A permanent cookie persists until its
               specified expiry time, defined by the Expires or Max-Age cookie
               attributes, while a session cookie is deleted when the current
               session ends.
               
               When set to "Permanent", AbsoluteTimeout indicates the
               cookie's lifetime via the Expires or Max-Age cookie attributes
               and is required.
               
               When set to "Session", AbsoluteTimeout indicates the
               absolute lifetime of the cookie tracked by the gateway and
               is optional.
               
               Defaults to "Session".
               
               Support: Core for "Session" type
               
               Support: Extended for "Permanent" type
        """
        if lifetime_type is not None:
            pulumi.set(__self__, "lifetime_type", lifetime_type)

    @_builtins.property
    @pulumi.getter(name="lifetimeType")
    def lifetime_type(self) -> Optional[_builtins.str]:
        """
        LifetimeType specifies whether the cookie has a permanent or
        session-based lifetime. A permanent cookie persists until its
        specified expiry time, defined by the Expires or Max-Age cookie
        attributes, while a session cookie is deleted when the current
        session ends.

        When set to "Permanent", AbsoluteTimeout indicates the
        cookie's lifetime via the Expires or Max-Age cookie attributes
        and is required.

        When set to "Session", AbsoluteTimeout indicates the
        absolute lifetime of the cookie tracked by the gateway and
        is optional.

        Defaults to "Session".

        Support: Core for "Session" type

        Support: Extended for "Permanent" type
        """
        return pulumi.get(self, "lifetime_type")


@pulumi.output_type
class XBackendTrafficPolicySpecSessionPersistenceCookieConfigPatch(dict):
    """
    CookieConfig provides configuration settings that are specific
    to cookie-based session persistence.

    Support: Core
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lifetimeType":
            suggest = "lifetime_type"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicySpecSessionPersistenceCookieConfigPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicySpecSessionPersistenceCookieConfigPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicySpecSessionPersistenceCookieConfigPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 lifetime_type: Optional[_builtins.str] = None):
        """
        CookieConfig provides configuration settings that are specific
        to cookie-based session persistence.

        Support: Core
        :param _builtins.str lifetime_type: LifetimeType specifies whether the cookie has a permanent or
               session-based lifetime. A permanent cookie persists until its
               specified expiry time, defined by the Expires or Max-Age cookie
               attributes, while a session cookie is deleted when the current
               session ends.
               
               When set to "Permanent", AbsoluteTimeout indicates the
               cookie's lifetime via the Expires or Max-Age cookie attributes
               and is required.
               
               When set to "Session", AbsoluteTimeout indicates the
               absolute lifetime of the cookie tracked by the gateway and
               is optional.
               
               Defaults to "Session".
               
               Support: Core for "Session" type
               
               Support: Extended for "Permanent" type
        """
        if lifetime_type is not None:
            pulumi.set(__self__, "lifetime_type", lifetime_type)

    @_builtins.property
    @pulumi.getter(name="lifetimeType")
    def lifetime_type(self) -> Optional[_builtins.str]:
        """
        LifetimeType specifies whether the cookie has a permanent or
        session-based lifetime. A permanent cookie persists until its
        specified expiry time, defined by the Expires or Max-Age cookie
        attributes, while a session cookie is deleted when the current
        session ends.

        When set to "Permanent", AbsoluteTimeout indicates the
        cookie's lifetime via the Expires or Max-Age cookie attributes
        and is required.

        When set to "Session", AbsoluteTimeout indicates the
        absolute lifetime of the cookie tracked by the gateway and
        is optional.

        Defaults to "Session".

        Support: Core for "Session" type

        Support: Extended for "Permanent" type
        """
        return pulumi.get(self, "lifetime_type")


@pulumi.output_type
class XBackendTrafficPolicySpecSessionPersistencePatch(dict):
    """
    SessionPersistence defines and configures session persistence
    for the backend.

    Support: Extended
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "absoluteTimeout":
            suggest = "absolute_timeout"
        elif key == "cookieConfig":
            suggest = "cookie_config"
        elif key == "idleTimeout":
            suggest = "idle_timeout"
        elif key == "sessionName":
            suggest = "session_name"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicySpecSessionPersistencePatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicySpecSessionPersistencePatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicySpecSessionPersistencePatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 absolute_timeout: Optional[_builtins.str] = None,
                 cookie_config: Optional['outputs.XBackendTrafficPolicySpecSessionPersistenceCookieConfigPatch'] = None,
                 idle_timeout: Optional[_builtins.str] = None,
                 session_name: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        SessionPersistence defines and configures session persistence
        for the backend.

        Support: Extended
        :param _builtins.str absolute_timeout: AbsoluteTimeout defines the absolute timeout of the persistent
               session. Once the AbsoluteTimeout duration has elapsed, the
               session becomes invalid.
               
               Support: Extended
        :param _builtins.str idle_timeout: IdleTimeout defines the idle timeout of the persistent session.
               Once the session has been idle for more than the specified
               IdleTimeout duration, the session becomes invalid.
               
               Support: Extended
        :param _builtins.str session_name: SessionName defines the name of the persistent session token
               which may be reflected in the cookie or the header. Users
               should avoid reusing session names to prevent unintended
               consequences, such as rejection or unpredictable behavior.
               
               Support: Implementation-specific
        :param _builtins.str type: Type defines the type of session persistence such as through
               the use a header or cookie. Defaults to cookie based session
               persistence.
               
               Support: Core for "Cookie" type
               
               Support: Extended for "Header" type
        """
        if absolute_timeout is not None:
            pulumi.set(__self__, "absolute_timeout", absolute_timeout)
        if cookie_config is not None:
            pulumi.set(__self__, "cookie_config", cookie_config)
        if idle_timeout is not None:
            pulumi.set(__self__, "idle_timeout", idle_timeout)
        if session_name is not None:
            pulumi.set(__self__, "session_name", session_name)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="absoluteTimeout")
    def absolute_timeout(self) -> Optional[_builtins.str]:
        """
        AbsoluteTimeout defines the absolute timeout of the persistent
        session. Once the AbsoluteTimeout duration has elapsed, the
        session becomes invalid.

        Support: Extended
        """
        return pulumi.get(self, "absolute_timeout")

    @_builtins.property
    @pulumi.getter(name="cookieConfig")
    def cookie_config(self) -> Optional['outputs.XBackendTrafficPolicySpecSessionPersistenceCookieConfigPatch']:
        return pulumi.get(self, "cookie_config")

    @_builtins.property
    @pulumi.getter(name="idleTimeout")
    def idle_timeout(self) -> Optional[_builtins.str]:
        """
        IdleTimeout defines the idle timeout of the persistent session.
        Once the session has been idle for more than the specified
        IdleTimeout duration, the session becomes invalid.

        Support: Extended
        """
        return pulumi.get(self, "idle_timeout")

    @_builtins.property
    @pulumi.getter(name="sessionName")
    def session_name(self) -> Optional[_builtins.str]:
        """
        SessionName defines the name of the persistent session token
        which may be reflected in the cookie or the header. Users
        should avoid reusing session names to prevent unintended
        consequences, such as rejection or unpredictable behavior.

        Support: Implementation-specific
        """
        return pulumi.get(self, "session_name")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        Type defines the type of session persistence such as through
        the use a header or cookie. Defaults to cookie based session
        persistence.

        Support: Core for "Cookie" type

        Support: Extended for "Header" type
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XBackendTrafficPolicySpecTargetRefs(dict):
    """
    LocalPolicyTargetReference identifies an API object to apply a direct or
    inherited policy to. This should be used as part of Policy resources
    that can target Gateway API resources. For more information on how this
    policy attachment model works, and a sample Policy resource, refer to
    the policy attachment documentation for Gateway API.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None):
        """
        LocalPolicyTargetReference identifies an API object to apply a direct or
        inherited policy to. This should be used as part of Policy resources
        that can target Gateway API resources. For more information on how this
        policy attachment model works, and a sample Policy resource, refer to
        the policy attachment documentation for Gateway API.
        :param _builtins.str group: Group is the group of the target resource.
        :param _builtins.str kind: Kind is kind of the target resource.
        :param _builtins.str name: Name is the name of the target resource.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the target resource.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the target resource.
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the target resource.
        """
        return pulumi.get(self, "name")


@pulumi.output_type
class XBackendTrafficPolicySpecTargetRefsPatch(dict):
    """
    LocalPolicyTargetReference identifies an API object to apply a direct or
    inherited policy to. This should be used as part of Policy resources
    that can target Gateway API resources. For more information on how this
    policy attachment model works, and a sample Policy resource, refer to
    the policy attachment documentation for Gateway API.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None):
        """
        LocalPolicyTargetReference identifies an API object to apply a direct or
        inherited policy to. This should be used as part of Policy resources
        that can target Gateway API resources. For more information on how this
        policy attachment model works, and a sample Policy resource, refer to
        the policy attachment documentation for Gateway API.
        :param _builtins.str group: Group is the group of the target resource.
        :param _builtins.str kind: Kind is kind of the target resource.
        :param _builtins.str name: Name is the name of the target resource.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the target resource.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the target resource.
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the target resource.
        """
        return pulumi.get(self, "name")


@pulumi.output_type
class XBackendTrafficPolicyStatus(dict):
    """
    Status defines the current state of BackendTrafficPolicy.
    """
    def __init__(__self__, *,
                 ancestors: Optional[Sequence['outputs.XBackendTrafficPolicyStatusAncestors']] = None):
        """
        Status defines the current state of BackendTrafficPolicy.
        :param Sequence['XBackendTrafficPolicyStatusAncestorsArgs'] ancestors: Ancestors is a list of ancestor resources (usually Gateways) that are
               associated with the policy, and the status of the policy with respect to
               each ancestor. When this policy attaches to a parent, the controller that
               manages the parent and the ancestors MUST add an entry to this list when
               the controller first sees the policy and SHOULD update the entry as
               appropriate when the relevant ancestor is modified.
               
               Note that choosing the relevant ancestor is left to the Policy designers;
               an important part of Policy design is designing the right object level at
               which to namespace this status.
               
               Note also that implementations MUST ONLY populate ancestor status for
               the Ancestor resources they are responsible for. Implementations MUST
               use the ControllerName field to uniquely identify the entries in this list
               that they are responsible for.
               
               Note that to achieve this, the list of PolicyAncestorStatus structs
               MUST be treated as a map with a composite key, made up of the AncestorRef
               and ControllerName fields combined.
               
               A maximum of 16 ancestors will be represented in this list. An empty list
               means the Policy is not relevant for any ancestors.
               
               If this slice is full, implementations MUST NOT add further entries.
               Instead they MUST consider the policy unimplementable and signal that
               on any related resources such as the ancestor that would be referenced
               here. For example, if this list was full on BackendTLSPolicy, no
               additional Gateways would be able to reference the Service targeted by
               the BackendTLSPolicy.
        """
        if ancestors is not None:
            pulumi.set(__self__, "ancestors", ancestors)

    @_builtins.property
    @pulumi.getter
    def ancestors(self) -> Optional[Sequence['outputs.XBackendTrafficPolicyStatusAncestors']]:
        """
        Ancestors is a list of ancestor resources (usually Gateways) that are
        associated with the policy, and the status of the policy with respect to
        each ancestor. When this policy attaches to a parent, the controller that
        manages the parent and the ancestors MUST add an entry to this list when
        the controller first sees the policy and SHOULD update the entry as
        appropriate when the relevant ancestor is modified.

        Note that choosing the relevant ancestor is left to the Policy designers;
        an important part of Policy design is designing the right object level at
        which to namespace this status.

        Note also that implementations MUST ONLY populate ancestor status for
        the Ancestor resources they are responsible for. Implementations MUST
        use the ControllerName field to uniquely identify the entries in this list
        that they are responsible for.

        Note that to achieve this, the list of PolicyAncestorStatus structs
        MUST be treated as a map with a composite key, made up of the AncestorRef
        and ControllerName fields combined.

        A maximum of 16 ancestors will be represented in this list. An empty list
        means the Policy is not relevant for any ancestors.

        If this slice is full, implementations MUST NOT add further entries.
        Instead they MUST consider the policy unimplementable and signal that
        on any related resources such as the ancestor that would be referenced
        here. For example, if this list was full on BackendTLSPolicy, no
        additional Gateways would be able to reference the Service targeted by
        the BackendTLSPolicy.
        """
        return pulumi.get(self, "ancestors")


@pulumi.output_type
class XBackendTrafficPolicyStatusAncestors(dict):
    """
    PolicyAncestorStatus describes the status of a route with respect to an
    associated Ancestor.

    Ancestors refer to objects that are either the Target of a policy or above it
    in terms of object hierarchy. For example, if a policy targets a Service, the
    Policy's Ancestors are, in order, the Service, the HTTPRoute, the Gateway, and
    the GatewayClass. Almost always, in this hierarchy, the Gateway will be the most
    useful object to place Policy status on, so we recommend that implementations
    SHOULD use Gateway as the PolicyAncestorStatus object unless the designers
    have a _very_ good reason otherwise.

    In the context of policy attachment, the Ancestor is used to distinguish which
    resource results in a distinct application of this policy. For example, if a policy
    targets a Service, it may have a distinct result per attached Gateway.

    Policies targeting the same resource may have different effects depending on the
    ancestors of those resources. For example, different Gateways targeting the same
    Service may have different capabilities, especially if they have different underlying
    implementations.

    For example, in BackendTLSPolicy, the Policy attaches to a Service that is
    used as a backend in a HTTPRoute that is itself attached to a Gateway.
    In this case, the relevant object for status is the Gateway, and that is the
    ancestor object referred to in this status.

    Note that a parent is also an ancestor, so for objects where the parent is the
    relevant object for status, this struct SHOULD still be used.

    This struct is intended to be used in a slice that's effectively a map,
    with a composite key made up of the AncestorRef and the ControllerName.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "ancestorRef":
            suggest = "ancestor_ref"
        elif key == "controllerName":
            suggest = "controller_name"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicyStatusAncestors. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicyStatusAncestors.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicyStatusAncestors.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 ancestor_ref: Optional['outputs.XBackendTrafficPolicyStatusAncestorsAncestorRef'] = None,
                 conditions: Optional[Sequence['outputs.XBackendTrafficPolicyStatusAncestorsConditions']] = None,
                 controller_name: Optional[_builtins.str] = None):
        """
        PolicyAncestorStatus describes the status of a route with respect to an
        associated Ancestor.

        Ancestors refer to objects that are either the Target of a policy or above it
        in terms of object hierarchy. For example, if a policy targets a Service, the
        Policy's Ancestors are, in order, the Service, the HTTPRoute, the Gateway, and
        the GatewayClass. Almost always, in this hierarchy, the Gateway will be the most
        useful object to place Policy status on, so we recommend that implementations
        SHOULD use Gateway as the PolicyAncestorStatus object unless the designers
        have a _very_ good reason otherwise.

        In the context of policy attachment, the Ancestor is used to distinguish which
        resource results in a distinct application of this policy. For example, if a policy
        targets a Service, it may have a distinct result per attached Gateway.

        Policies targeting the same resource may have different effects depending on the
        ancestors of those resources. For example, different Gateways targeting the same
        Service may have different capabilities, especially if they have different underlying
        implementations.

        For example, in BackendTLSPolicy, the Policy attaches to a Service that is
        used as a backend in a HTTPRoute that is itself attached to a Gateway.
        In this case, the relevant object for status is the Gateway, and that is the
        ancestor object referred to in this status.

        Note that a parent is also an ancestor, so for objects where the parent is the
        relevant object for status, this struct SHOULD still be used.

        This struct is intended to be used in a slice that's effectively a map,
        with a composite key made up of the AncestorRef and the ControllerName.
        :param Sequence['XBackendTrafficPolicyStatusAncestorsConditionsArgs'] conditions: Conditions describes the status of the Policy with respect to the given Ancestor.
        :param _builtins.str controller_name: ControllerName is a domain/path string that indicates the name of the
               controller that wrote this status. This corresponds with the
               controllerName field on GatewayClass.
               
               Example: "example.net/gateway-controller".
               
               The format of this field is DOMAIN "/" PATH, where DOMAIN and PATH are
               valid Kubernetes names
               (https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names).
               
               Controllers MUST populate this field when writing status. Controllers should ensure that
               entries to status populated with their ControllerName are cleaned up when they are no
               longer necessary.
        """
        if ancestor_ref is not None:
            pulumi.set(__self__, "ancestor_ref", ancestor_ref)
        if conditions is not None:
            pulumi.set(__self__, "conditions", conditions)
        if controller_name is not None:
            pulumi.set(__self__, "controller_name", controller_name)

    @_builtins.property
    @pulumi.getter(name="ancestorRef")
    def ancestor_ref(self) -> Optional['outputs.XBackendTrafficPolicyStatusAncestorsAncestorRef']:
        return pulumi.get(self, "ancestor_ref")

    @_builtins.property
    @pulumi.getter
    def conditions(self) -> Optional[Sequence['outputs.XBackendTrafficPolicyStatusAncestorsConditions']]:
        """
        Conditions describes the status of the Policy with respect to the given Ancestor.
        """
        return pulumi.get(self, "conditions")

    @_builtins.property
    @pulumi.getter(name="controllerName")
    def controller_name(self) -> Optional[_builtins.str]:
        """
        ControllerName is a domain/path string that indicates the name of the
        controller that wrote this status. This corresponds with the
        controllerName field on GatewayClass.

        Example: "example.net/gateway-controller".

        The format of this field is DOMAIN "/" PATH, where DOMAIN and PATH are
        valid Kubernetes names
        (https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names).

        Controllers MUST populate this field when writing status. Controllers should ensure that
        entries to status populated with their ControllerName are cleaned up when they are no
        longer necessary.
        """
        return pulumi.get(self, "controller_name")


@pulumi.output_type
class XBackendTrafficPolicyStatusAncestorsAncestorRef(dict):
    """
    AncestorRef corresponds with a ParentRef in the spec that this
    PolicyAncestorStatus struct describes the status of.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "sectionName":
            suggest = "section_name"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicyStatusAncestorsAncestorRef. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicyStatusAncestorsAncestorRef.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicyStatusAncestorsAncestorRef.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 namespace: Optional[_builtins.str] = None,
                 port: Optional[_builtins.int] = None,
                 section_name: Optional[_builtins.str] = None):
        """
        AncestorRef corresponds with a ParentRef in the spec that this
        PolicyAncestorStatus struct describes the status of.
        :param _builtins.str group: Group is the group of the referent.
               When unspecified, "gateway.networking.k8s.io" is inferred.
               To set the core API group (such as for a "Service" kind referent),
               Group must be explicitly set to "" (empty string).
               
               Support: Core
        :param _builtins.str kind: Kind is kind of the referent.
               
               There are two kinds of parent resources with "Core" support:
               
               * Gateway (Gateway conformance profile)
               * Service (Mesh conformance profile, ClusterIP Services only)
               
               Support for other resources is Implementation-Specific.
        :param _builtins.str name: Name is the name of the referent.
               
               Support: Core
        :param _builtins.str namespace: Namespace is the namespace of the referent. When unspecified, this refers
               to the local namespace of the Route.
               
               Note that there are specific rules for ParentRefs which cross namespace
               boundaries. Cross-namespace references are only valid if they are explicitly
               allowed by something in the namespace they are referring to. For example:
               Gateway has the AllowedRoutes field, and ReferenceGrant provides a
               generic way to enable any other kind of cross-namespace reference.
               
               
               ParentRefs from a Route to a Service in the same namespace are "producer"
               routes, which apply default routing rules to inbound connections from
               any namespace to the Service.
               
               ParentRefs from a Route to a Service in a different namespace are
               "consumer" routes, and these routing rules are only applied to outbound
               connections originating from the same namespace as the Route, for which
               the intended destination of the connections are a Service targeted as a
               ParentRef of the Route.
               
               
               Support: Core
        :param _builtins.int port: Port is the network port this Route targets. It can be interpreted
               differently based on the type of parent resource.
               
               When the parent resource is a Gateway, this targets all listeners
               listening on the specified port that also support this kind of Route(and
               select this Route). It's not recommended to set `Port` unless the
               networking behaviors specified in a Route must apply to a specific port
               as opposed to a listener(s) whose port(s) may be changed. When both Port
               and SectionName are specified, the name and port of the selected listener
               must match both specified values.
               
               
               When the parent resource is a Service, this targets a specific port in the
               Service spec. When both Port (experimental) and SectionName are specified,
               the name and port of the selected port must match both specified values.
               
               
               Implementations MAY choose to support other parent resources.
               Implementations supporting other types of parent resources MUST clearly
               document how/if Port is interpreted.
               
               For the purpose of status, an attachment is considered successful as
               long as the parent resource accepts it partially. For example, Gateway
               listeners can restrict which Routes can attach to them by Route kind,
               namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
               from the referencing Route, the Route MUST be considered successfully
               attached. If no Gateway listeners accept attachment from this Route,
               the Route MUST be considered detached from the Gateway.
               
               Support: Extended
        :param _builtins.str section_name: SectionName is the name of a section within the target resource. In the
               following resources, SectionName is interpreted as the following:
               
               * Gateway: Listener name. When both Port (experimental) and SectionName
               are specified, the name and port of the selected listener must match
               both specified values.
               * Service: Port name. When both Port (experimental) and SectionName
               are specified, the name and port of the selected listener must match
               both specified values.
               
               Implementations MAY choose to support attaching Routes to other resources.
               If that is the case, they MUST clearly document how SectionName is
               interpreted.
               
               When unspecified (empty string), this will reference the entire resource.
               For the purpose of status, an attachment is considered successful if at
               least one section in the parent resource accepts it. For example, Gateway
               listeners can restrict which Routes can attach to them by Route kind,
               namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
               the referencing Route, the Route MUST be considered successfully
               attached. If no Gateway listeners accept attachment from this Route, the
               Route MUST be considered detached from the Gateway.
               
               Support: Core
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)
        if port is not None:
            pulumi.set(__self__, "port", port)
        if section_name is not None:
            pulumi.set(__self__, "section_name", section_name)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the referent.
        When unspecified, "gateway.networking.k8s.io" is inferred.
        To set the core API group (such as for a "Service" kind referent),
        Group must be explicitly set to "" (empty string).

        Support: Core
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the referent.

        There are two kinds of parent resources with "Core" support:

        * Gateway (Gateway conformance profile)
        * Service (Mesh conformance profile, ClusterIP Services only)

        Support for other resources is Implementation-Specific.
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the referent.

        Support: Core
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        """
        Namespace is the namespace of the referent. When unspecified, this refers
        to the local namespace of the Route.

        Note that there are specific rules for ParentRefs which cross namespace
        boundaries. Cross-namespace references are only valid if they are explicitly
        allowed by something in the namespace they are referring to. For example:
        Gateway has the AllowedRoutes field, and ReferenceGrant provides a
        generic way to enable any other kind of cross-namespace reference.


        ParentRefs from a Route to a Service in the same namespace are "producer"
        routes, which apply default routing rules to inbound connections from
        any namespace to the Service.

        ParentRefs from a Route to a Service in a different namespace are
        "consumer" routes, and these routing rules are only applied to outbound
        connections originating from the same namespace as the Route, for which
        the intended destination of the connections are a Service targeted as a
        ParentRef of the Route.


        Support: Core
        """
        return pulumi.get(self, "namespace")

    @_builtins.property
    @pulumi.getter
    def port(self) -> Optional[_builtins.int]:
        """
        Port is the network port this Route targets. It can be interpreted
        differently based on the type of parent resource.

        When the parent resource is a Gateway, this targets all listeners
        listening on the specified port that also support this kind of Route(and
        select this Route). It's not recommended to set `Port` unless the
        networking behaviors specified in a Route must apply to a specific port
        as opposed to a listener(s) whose port(s) may be changed. When both Port
        and SectionName are specified, the name and port of the selected listener
        must match both specified values.


        When the parent resource is a Service, this targets a specific port in the
        Service spec. When both Port (experimental) and SectionName are specified,
        the name and port of the selected port must match both specified values.


        Implementations MAY choose to support other parent resources.
        Implementations supporting other types of parent resources MUST clearly
        document how/if Port is interpreted.

        For the purpose of status, an attachment is considered successful as
        long as the parent resource accepts it partially. For example, Gateway
        listeners can restrict which Routes can attach to them by Route kind,
        namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
        from the referencing Route, the Route MUST be considered successfully
        attached. If no Gateway listeners accept attachment from this Route,
        the Route MUST be considered detached from the Gateway.

        Support: Extended
        """
        return pulumi.get(self, "port")

    @_builtins.property
    @pulumi.getter(name="sectionName")
    def section_name(self) -> Optional[_builtins.str]:
        """
        SectionName is the name of a section within the target resource. In the
        following resources, SectionName is interpreted as the following:

        * Gateway: Listener name. When both Port (experimental) and SectionName
        are specified, the name and port of the selected listener must match
        both specified values.
        * Service: Port name. When both Port (experimental) and SectionName
        are specified, the name and port of the selected listener must match
        both specified values.

        Implementations MAY choose to support attaching Routes to other resources.
        If that is the case, they MUST clearly document how SectionName is
        interpreted.

        When unspecified (empty string), this will reference the entire resource.
        For the purpose of status, an attachment is considered successful if at
        least one section in the parent resource accepts it. For example, Gateway
        listeners can restrict which Routes can attach to them by Route kind,
        namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
        the referencing Route, the Route MUST be considered successfully
        attached. If no Gateway listeners accept attachment from this Route, the
        Route MUST be considered detached from the Gateway.

        Support: Core
        """
        return pulumi.get(self, "section_name")


@pulumi.output_type
class XBackendTrafficPolicyStatusAncestorsAncestorRefPatch(dict):
    """
    AncestorRef corresponds with a ParentRef in the spec that this
    PolicyAncestorStatus struct describes the status of.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "sectionName":
            suggest = "section_name"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicyStatusAncestorsAncestorRefPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicyStatusAncestorsAncestorRefPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicyStatusAncestorsAncestorRefPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 namespace: Optional[_builtins.str] = None,
                 port: Optional[_builtins.int] = None,
                 section_name: Optional[_builtins.str] = None):
        """
        AncestorRef corresponds with a ParentRef in the spec that this
        PolicyAncestorStatus struct describes the status of.
        :param _builtins.str group: Group is the group of the referent.
               When unspecified, "gateway.networking.k8s.io" is inferred.
               To set the core API group (such as for a "Service" kind referent),
               Group must be explicitly set to "" (empty string).
               
               Support: Core
        :param _builtins.str kind: Kind is kind of the referent.
               
               There are two kinds of parent resources with "Core" support:
               
               * Gateway (Gateway conformance profile)
               * Service (Mesh conformance profile, ClusterIP Services only)
               
               Support for other resources is Implementation-Specific.
        :param _builtins.str name: Name is the name of the referent.
               
               Support: Core
        :param _builtins.str namespace: Namespace is the namespace of the referent. When unspecified, this refers
               to the local namespace of the Route.
               
               Note that there are specific rules for ParentRefs which cross namespace
               boundaries. Cross-namespace references are only valid if they are explicitly
               allowed by something in the namespace they are referring to. For example:
               Gateway has the AllowedRoutes field, and ReferenceGrant provides a
               generic way to enable any other kind of cross-namespace reference.
               
               
               ParentRefs from a Route to a Service in the same namespace are "producer"
               routes, which apply default routing rules to inbound connections from
               any namespace to the Service.
               
               ParentRefs from a Route to a Service in a different namespace are
               "consumer" routes, and these routing rules are only applied to outbound
               connections originating from the same namespace as the Route, for which
               the intended destination of the connections are a Service targeted as a
               ParentRef of the Route.
               
               
               Support: Core
        :param _builtins.int port: Port is the network port this Route targets. It can be interpreted
               differently based on the type of parent resource.
               
               When the parent resource is a Gateway, this targets all listeners
               listening on the specified port that also support this kind of Route(and
               select this Route). It's not recommended to set `Port` unless the
               networking behaviors specified in a Route must apply to a specific port
               as opposed to a listener(s) whose port(s) may be changed. When both Port
               and SectionName are specified, the name and port of the selected listener
               must match both specified values.
               
               
               When the parent resource is a Service, this targets a specific port in the
               Service spec. When both Port (experimental) and SectionName are specified,
               the name and port of the selected port must match both specified values.
               
               
               Implementations MAY choose to support other parent resources.
               Implementations supporting other types of parent resources MUST clearly
               document how/if Port is interpreted.
               
               For the purpose of status, an attachment is considered successful as
               long as the parent resource accepts it partially. For example, Gateway
               listeners can restrict which Routes can attach to them by Route kind,
               namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
               from the referencing Route, the Route MUST be considered successfully
               attached. If no Gateway listeners accept attachment from this Route,
               the Route MUST be considered detached from the Gateway.
               
               Support: Extended
        :param _builtins.str section_name: SectionName is the name of a section within the target resource. In the
               following resources, SectionName is interpreted as the following:
               
               * Gateway: Listener name. When both Port (experimental) and SectionName
               are specified, the name and port of the selected listener must match
               both specified values.
               * Service: Port name. When both Port (experimental) and SectionName
               are specified, the name and port of the selected listener must match
               both specified values.
               
               Implementations MAY choose to support attaching Routes to other resources.
               If that is the case, they MUST clearly document how SectionName is
               interpreted.
               
               When unspecified (empty string), this will reference the entire resource.
               For the purpose of status, an attachment is considered successful if at
               least one section in the parent resource accepts it. For example, Gateway
               listeners can restrict which Routes can attach to them by Route kind,
               namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
               the referencing Route, the Route MUST be considered successfully
               attached. If no Gateway listeners accept attachment from this Route, the
               Route MUST be considered detached from the Gateway.
               
               Support: Core
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)
        if port is not None:
            pulumi.set(__self__, "port", port)
        if section_name is not None:
            pulumi.set(__self__, "section_name", section_name)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the referent.
        When unspecified, "gateway.networking.k8s.io" is inferred.
        To set the core API group (such as for a "Service" kind referent),
        Group must be explicitly set to "" (empty string).

        Support: Core
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the referent.

        There are two kinds of parent resources with "Core" support:

        * Gateway (Gateway conformance profile)
        * Service (Mesh conformance profile, ClusterIP Services only)

        Support for other resources is Implementation-Specific.
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the referent.

        Support: Core
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        """
        Namespace is the namespace of the referent. When unspecified, this refers
        to the local namespace of the Route.

        Note that there are specific rules for ParentRefs which cross namespace
        boundaries. Cross-namespace references are only valid if they are explicitly
        allowed by something in the namespace they are referring to. For example:
        Gateway has the AllowedRoutes field, and ReferenceGrant provides a
        generic way to enable any other kind of cross-namespace reference.


        ParentRefs from a Route to a Service in the same namespace are "producer"
        routes, which apply default routing rules to inbound connections from
        any namespace to the Service.

        ParentRefs from a Route to a Service in a different namespace are
        "consumer" routes, and these routing rules are only applied to outbound
        connections originating from the same namespace as the Route, for which
        the intended destination of the connections are a Service targeted as a
        ParentRef of the Route.


        Support: Core
        """
        return pulumi.get(self, "namespace")

    @_builtins.property
    @pulumi.getter
    def port(self) -> Optional[_builtins.int]:
        """
        Port is the network port this Route targets. It can be interpreted
        differently based on the type of parent resource.

        When the parent resource is a Gateway, this targets all listeners
        listening on the specified port that also support this kind of Route(and
        select this Route). It's not recommended to set `Port` unless the
        networking behaviors specified in a Route must apply to a specific port
        as opposed to a listener(s) whose port(s) may be changed. When both Port
        and SectionName are specified, the name and port of the selected listener
        must match both specified values.


        When the parent resource is a Service, this targets a specific port in the
        Service spec. When both Port (experimental) and SectionName are specified,
        the name and port of the selected port must match both specified values.


        Implementations MAY choose to support other parent resources.
        Implementations supporting other types of parent resources MUST clearly
        document how/if Port is interpreted.

        For the purpose of status, an attachment is considered successful as
        long as the parent resource accepts it partially. For example, Gateway
        listeners can restrict which Routes can attach to them by Route kind,
        namespace, or hostname. If 1 of 2 Gateway listeners accept attachment
        from the referencing Route, the Route MUST be considered successfully
        attached. If no Gateway listeners accept attachment from this Route,
        the Route MUST be considered detached from the Gateway.

        Support: Extended
        """
        return pulumi.get(self, "port")

    @_builtins.property
    @pulumi.getter(name="sectionName")
    def section_name(self) -> Optional[_builtins.str]:
        """
        SectionName is the name of a section within the target resource. In the
        following resources, SectionName is interpreted as the following:

        * Gateway: Listener name. When both Port (experimental) and SectionName
        are specified, the name and port of the selected listener must match
        both specified values.
        * Service: Port name. When both Port (experimental) and SectionName
        are specified, the name and port of the selected listener must match
        both specified values.

        Implementations MAY choose to support attaching Routes to other resources.
        If that is the case, they MUST clearly document how SectionName is
        interpreted.

        When unspecified (empty string), this will reference the entire resource.
        For the purpose of status, an attachment is considered successful if at
        least one section in the parent resource accepts it. For example, Gateway
        listeners can restrict which Routes can attach to them by Route kind,
        namespace, or hostname. If 1 of 2 Gateway listeners accept attachment from
        the referencing Route, the Route MUST be considered successfully
        attached. If no Gateway listeners accept attachment from this Route, the
        Route MUST be considered detached from the Gateway.

        Support: Core
        """
        return pulumi.get(self, "section_name")


@pulumi.output_type
class XBackendTrafficPolicyStatusAncestorsConditions(dict):
    """
    Condition contains details for one aspect of the current state of this API Resource.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lastTransitionTime":
            suggest = "last_transition_time"
        elif key == "observedGeneration":
            suggest = "observed_generation"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicyStatusAncestorsConditions. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicyStatusAncestorsConditions.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicyStatusAncestorsConditions.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 last_transition_time: Optional[_builtins.str] = None,
                 message: Optional[_builtins.str] = None,
                 observed_generation: Optional[_builtins.int] = None,
                 reason: Optional[_builtins.str] = None,
                 status: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        Condition contains details for one aspect of the current state of this API Resource.
        :param _builtins.str last_transition_time: lastTransitionTime is the last time the condition transitioned from one status to another.
               This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        :param _builtins.str message: message is a human readable message indicating details about the transition.
               This may be an empty string.
        :param _builtins.int observed_generation: observedGeneration represents the .metadata.generation that the condition was set based upon.
               For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
               with respect to the current state of the instance.
        :param _builtins.str reason: reason contains a programmatic identifier indicating the reason for the condition's last transition.
               Producers of specific condition types may define expected values and meanings for this field,
               and whether the values are considered a guaranteed API.
               The value should be a CamelCase string.
               This field may not be empty.
        :param _builtins.str status: status of the condition, one of True, False, Unknown.
        :param _builtins.str type: type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        if last_transition_time is not None:
            pulumi.set(__self__, "last_transition_time", last_transition_time)
        if message is not None:
            pulumi.set(__self__, "message", message)
        if observed_generation is not None:
            pulumi.set(__self__, "observed_generation", observed_generation)
        if reason is not None:
            pulumi.set(__self__, "reason", reason)
        if status is not None:
            pulumi.set(__self__, "status", status)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="lastTransitionTime")
    def last_transition_time(self) -> Optional[_builtins.str]:
        """
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        """
        return pulumi.get(self, "last_transition_time")

    @_builtins.property
    @pulumi.getter
    def message(self) -> Optional[_builtins.str]:
        """
        message is a human readable message indicating details about the transition.
        This may be an empty string.
        """
        return pulumi.get(self, "message")

    @_builtins.property
    @pulumi.getter(name="observedGeneration")
    def observed_generation(self) -> Optional[_builtins.int]:
        """
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
        """
        return pulumi.get(self, "observed_generation")

    @_builtins.property
    @pulumi.getter
    def reason(self) -> Optional[_builtins.str]:
        """
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
        """
        return pulumi.get(self, "reason")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        status of the condition, one of True, False, Unknown.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XBackendTrafficPolicyStatusAncestorsConditionsPatch(dict):
    """
    Condition contains details for one aspect of the current state of this API Resource.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lastTransitionTime":
            suggest = "last_transition_time"
        elif key == "observedGeneration":
            suggest = "observed_generation"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicyStatusAncestorsConditionsPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicyStatusAncestorsConditionsPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicyStatusAncestorsConditionsPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 last_transition_time: Optional[_builtins.str] = None,
                 message: Optional[_builtins.str] = None,
                 observed_generation: Optional[_builtins.int] = None,
                 reason: Optional[_builtins.str] = None,
                 status: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        Condition contains details for one aspect of the current state of this API Resource.
        :param _builtins.str last_transition_time: lastTransitionTime is the last time the condition transitioned from one status to another.
               This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        :param _builtins.str message: message is a human readable message indicating details about the transition.
               This may be an empty string.
        :param _builtins.int observed_generation: observedGeneration represents the .metadata.generation that the condition was set based upon.
               For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
               with respect to the current state of the instance.
        :param _builtins.str reason: reason contains a programmatic identifier indicating the reason for the condition's last transition.
               Producers of specific condition types may define expected values and meanings for this field,
               and whether the values are considered a guaranteed API.
               The value should be a CamelCase string.
               This field may not be empty.
        :param _builtins.str status: status of the condition, one of True, False, Unknown.
        :param _builtins.str type: type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        if last_transition_time is not None:
            pulumi.set(__self__, "last_transition_time", last_transition_time)
        if message is not None:
            pulumi.set(__self__, "message", message)
        if observed_generation is not None:
            pulumi.set(__self__, "observed_generation", observed_generation)
        if reason is not None:
            pulumi.set(__self__, "reason", reason)
        if status is not None:
            pulumi.set(__self__, "status", status)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="lastTransitionTime")
    def last_transition_time(self) -> Optional[_builtins.str]:
        """
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        """
        return pulumi.get(self, "last_transition_time")

    @_builtins.property
    @pulumi.getter
    def message(self) -> Optional[_builtins.str]:
        """
        message is a human readable message indicating details about the transition.
        This may be an empty string.
        """
        return pulumi.get(self, "message")

    @_builtins.property
    @pulumi.getter(name="observedGeneration")
    def observed_generation(self) -> Optional[_builtins.int]:
        """
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
        """
        return pulumi.get(self, "observed_generation")

    @_builtins.property
    @pulumi.getter
    def reason(self) -> Optional[_builtins.str]:
        """
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
        """
        return pulumi.get(self, "reason")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        status of the condition, one of True, False, Unknown.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XBackendTrafficPolicyStatusAncestorsPatch(dict):
    """
    PolicyAncestorStatus describes the status of a route with respect to an
    associated Ancestor.

    Ancestors refer to objects that are either the Target of a policy or above it
    in terms of object hierarchy. For example, if a policy targets a Service, the
    Policy's Ancestors are, in order, the Service, the HTTPRoute, the Gateway, and
    the GatewayClass. Almost always, in this hierarchy, the Gateway will be the most
    useful object to place Policy status on, so we recommend that implementations
    SHOULD use Gateway as the PolicyAncestorStatus object unless the designers
    have a _very_ good reason otherwise.

    In the context of policy attachment, the Ancestor is used to distinguish which
    resource results in a distinct application of this policy. For example, if a policy
    targets a Service, it may have a distinct result per attached Gateway.

    Policies targeting the same resource may have different effects depending on the
    ancestors of those resources. For example, different Gateways targeting the same
    Service may have different capabilities, especially if they have different underlying
    implementations.

    For example, in BackendTLSPolicy, the Policy attaches to a Service that is
    used as a backend in a HTTPRoute that is itself attached to a Gateway.
    In this case, the relevant object for status is the Gateway, and that is the
    ancestor object referred to in this status.

    Note that a parent is also an ancestor, so for objects where the parent is the
    relevant object for status, this struct SHOULD still be used.

    This struct is intended to be used in a slice that's effectively a map,
    with a composite key made up of the AncestorRef and the ControllerName.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "ancestorRef":
            suggest = "ancestor_ref"
        elif key == "controllerName":
            suggest = "controller_name"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XBackendTrafficPolicyStatusAncestorsPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XBackendTrafficPolicyStatusAncestorsPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XBackendTrafficPolicyStatusAncestorsPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 ancestor_ref: Optional['outputs.XBackendTrafficPolicyStatusAncestorsAncestorRefPatch'] = None,
                 conditions: Optional[Sequence['outputs.XBackendTrafficPolicyStatusAncestorsConditionsPatch']] = None,
                 controller_name: Optional[_builtins.str] = None):
        """
        PolicyAncestorStatus describes the status of a route with respect to an
        associated Ancestor.

        Ancestors refer to objects that are either the Target of a policy or above it
        in terms of object hierarchy. For example, if a policy targets a Service, the
        Policy's Ancestors are, in order, the Service, the HTTPRoute, the Gateway, and
        the GatewayClass. Almost always, in this hierarchy, the Gateway will be the most
        useful object to place Policy status on, so we recommend that implementations
        SHOULD use Gateway as the PolicyAncestorStatus object unless the designers
        have a _very_ good reason otherwise.

        In the context of policy attachment, the Ancestor is used to distinguish which
        resource results in a distinct application of this policy. For example, if a policy
        targets a Service, it may have a distinct result per attached Gateway.

        Policies targeting the same resource may have different effects depending on the
        ancestors of those resources. For example, different Gateways targeting the same
        Service may have different capabilities, especially if they have different underlying
        implementations.

        For example, in BackendTLSPolicy, the Policy attaches to a Service that is
        used as a backend in a HTTPRoute that is itself attached to a Gateway.
        In this case, the relevant object for status is the Gateway, and that is the
        ancestor object referred to in this status.

        Note that a parent is also an ancestor, so for objects where the parent is the
        relevant object for status, this struct SHOULD still be used.

        This struct is intended to be used in a slice that's effectively a map,
        with a composite key made up of the AncestorRef and the ControllerName.
        :param Sequence['XBackendTrafficPolicyStatusAncestorsConditionsPatchArgs'] conditions: Conditions describes the status of the Policy with respect to the given Ancestor.
        :param _builtins.str controller_name: ControllerName is a domain/path string that indicates the name of the
               controller that wrote this status. This corresponds with the
               controllerName field on GatewayClass.
               
               Example: "example.net/gateway-controller".
               
               The format of this field is DOMAIN "/" PATH, where DOMAIN and PATH are
               valid Kubernetes names
               (https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names).
               
               Controllers MUST populate this field when writing status. Controllers should ensure that
               entries to status populated with their ControllerName are cleaned up when they are no
               longer necessary.
        """
        if ancestor_ref is not None:
            pulumi.set(__self__, "ancestor_ref", ancestor_ref)
        if conditions is not None:
            pulumi.set(__self__, "conditions", conditions)
        if controller_name is not None:
            pulumi.set(__self__, "controller_name", controller_name)

    @_builtins.property
    @pulumi.getter(name="ancestorRef")
    def ancestor_ref(self) -> Optional['outputs.XBackendTrafficPolicyStatusAncestorsAncestorRefPatch']:
        return pulumi.get(self, "ancestor_ref")

    @_builtins.property
    @pulumi.getter
    def conditions(self) -> Optional[Sequence['outputs.XBackendTrafficPolicyStatusAncestorsConditionsPatch']]:
        """
        Conditions describes the status of the Policy with respect to the given Ancestor.
        """
        return pulumi.get(self, "conditions")

    @_builtins.property
    @pulumi.getter(name="controllerName")
    def controller_name(self) -> Optional[_builtins.str]:
        """
        ControllerName is a domain/path string that indicates the name of the
        controller that wrote this status. This corresponds with the
        controllerName field on GatewayClass.

        Example: "example.net/gateway-controller".

        The format of this field is DOMAIN "/" PATH, where DOMAIN and PATH are
        valid Kubernetes names
        (https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names).

        Controllers MUST populate this field when writing status. Controllers should ensure that
        entries to status populated with their ControllerName are cleaned up when they are no
        longer necessary.
        """
        return pulumi.get(self, "controller_name")


@pulumi.output_type
class XBackendTrafficPolicyStatusPatch(dict):
    """
    Status defines the current state of BackendTrafficPolicy.
    """
    def __init__(__self__, *,
                 ancestors: Optional[Sequence['outputs.XBackendTrafficPolicyStatusAncestorsPatch']] = None):
        """
        Status defines the current state of BackendTrafficPolicy.
        :param Sequence['XBackendTrafficPolicyStatusAncestorsPatchArgs'] ancestors: Ancestors is a list of ancestor resources (usually Gateways) that are
               associated with the policy, and the status of the policy with respect to
               each ancestor. When this policy attaches to a parent, the controller that
               manages the parent and the ancestors MUST add an entry to this list when
               the controller first sees the policy and SHOULD update the entry as
               appropriate when the relevant ancestor is modified.
               
               Note that choosing the relevant ancestor is left to the Policy designers;
               an important part of Policy design is designing the right object level at
               which to namespace this status.
               
               Note also that implementations MUST ONLY populate ancestor status for
               the Ancestor resources they are responsible for. Implementations MUST
               use the ControllerName field to uniquely identify the entries in this list
               that they are responsible for.
               
               Note that to achieve this, the list of PolicyAncestorStatus structs
               MUST be treated as a map with a composite key, made up of the AncestorRef
               and ControllerName fields combined.
               
               A maximum of 16 ancestors will be represented in this list. An empty list
               means the Policy is not relevant for any ancestors.
               
               If this slice is full, implementations MUST NOT add further entries.
               Instead they MUST consider the policy unimplementable and signal that
               on any related resources such as the ancestor that would be referenced
               here. For example, if this list was full on BackendTLSPolicy, no
               additional Gateways would be able to reference the Service targeted by
               the BackendTLSPolicy.
        """
        if ancestors is not None:
            pulumi.set(__self__, "ancestors", ancestors)

    @_builtins.property
    @pulumi.getter
    def ancestors(self) -> Optional[Sequence['outputs.XBackendTrafficPolicyStatusAncestorsPatch']]:
        """
        Ancestors is a list of ancestor resources (usually Gateways) that are
        associated with the policy, and the status of the policy with respect to
        each ancestor. When this policy attaches to a parent, the controller that
        manages the parent and the ancestors MUST add an entry to this list when
        the controller first sees the policy and SHOULD update the entry as
        appropriate when the relevant ancestor is modified.

        Note that choosing the relevant ancestor is left to the Policy designers;
        an important part of Policy design is designing the right object level at
        which to namespace this status.

        Note also that implementations MUST ONLY populate ancestor status for
        the Ancestor resources they are responsible for. Implementations MUST
        use the ControllerName field to uniquely identify the entries in this list
        that they are responsible for.

        Note that to achieve this, the list of PolicyAncestorStatus structs
        MUST be treated as a map with a composite key, made up of the AncestorRef
        and ControllerName fields combined.

        A maximum of 16 ancestors will be represented in this list. An empty list
        means the Policy is not relevant for any ancestors.

        If this slice is full, implementations MUST NOT add further entries.
        Instead they MUST consider the policy unimplementable and signal that
        on any related resources such as the ancestor that would be referenced
        here. For example, if this list was full on BackendTLSPolicy, no
        additional Gateways would be able to reference the Service targeted by
        the BackendTLSPolicy.
        """
        return pulumi.get(self, "ancestors")


@pulumi.output_type
class XListenerSet(dict):
    """
    XListenerSet defines a set of additional listeners to attach to an existing Gateway.
    This resource provides a mechanism to merge multiple listeners into a single Gateway.

    The parent Gateway must explicitly allow ListenerSet attachment through its
    AllowedListeners configuration. By default, Gateways do not allow ListenerSet
    attachment.

    Routes can attach to a ListenerSet by specifying it as a parentRef, and can
    optionally target specific listeners using the sectionName field.

    Policy Attachment:
    - Policies that attach to a ListenerSet apply to all listeners defined in that resource
    - Policies do not impact listeners in the parent Gateway
    - Different ListenerSets attached to the same Gateway can have different policies
    - If an implementation cannot apply a policy to specific listeners, it should reject the policy

    ReferenceGrant Semantics:
    - ReferenceGrants applied to a Gateway are not inherited by child ListenerSets
    - ReferenceGrants applied to a ListenerSet do not grant permission to the parent Gateway's listeners
    - A ListenerSet can reference secrets/backends in its own namespace without a ReferenceGrant

    Gateway Integration:
    - The parent Gateway's status will include an "AttachedListenerSets" condition
    - This condition will be:
      - True: when AllowedListeners is set and at least one child ListenerSet is attached
      - False: when AllowedListeners is set but no valid listeners are attached, or when AllowedListeners is not set or false
      - Unknown: when no AllowedListeners config is present
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "apiVersion":
            suggest = "api_version"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSet. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSet.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSet.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 api_version: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 metadata: Optional['_meta.v1.outputs.ObjectMeta'] = None,
                 spec: Optional['outputs.XListenerSetSpec'] = None,
                 status: Optional['outputs.XListenerSetStatus'] = None):
        """
        XListenerSet defines a set of additional listeners to attach to an existing Gateway.
        This resource provides a mechanism to merge multiple listeners into a single Gateway.

        The parent Gateway must explicitly allow ListenerSet attachment through its
        AllowedListeners configuration. By default, Gateways do not allow ListenerSet
        attachment.

        Routes can attach to a ListenerSet by specifying it as a parentRef, and can
        optionally target specific listeners using the sectionName field.

        Policy Attachment:
        - Policies that attach to a ListenerSet apply to all listeners defined in that resource
        - Policies do not impact listeners in the parent Gateway
        - Different ListenerSets attached to the same Gateway can have different policies
        - If an implementation cannot apply a policy to specific listeners, it should reject the policy

        ReferenceGrant Semantics:
        - ReferenceGrants applied to a Gateway are not inherited by child ListenerSets
        - ReferenceGrants applied to a ListenerSet do not grant permission to the parent Gateway's listeners
        - A ListenerSet can reference secrets/backends in its own namespace without a ReferenceGrant

        Gateway Integration:
        - The parent Gateway's status will include an "AttachedListenerSets" condition
        - This condition will be:
          - True: when AllowedListeners is set and at least one child ListenerSet is attached
          - False: when AllowedListeners is set but no valid listeners are attached, or when AllowedListeners is not set or false
          - Unknown: when no AllowedListeners config is present
        :param _builtins.str api_version: APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        :param _builtins.str kind: Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        :param '_meta.v1.ObjectMetaArgs' metadata: Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
        """
        if api_version is not None:
            pulumi.set(__self__, "api_version", 'gateway.networking.x-k8s.io/v1alpha1')
        if kind is not None:
            pulumi.set(__self__, "kind", 'XListenerSet')
        if metadata is not None:
            pulumi.set(__self__, "metadata", metadata)
        if spec is not None:
            pulumi.set(__self__, "spec", spec)
        if status is not None:
            pulumi.set(__self__, "status", status)

    @_builtins.property
    @pulumi.getter(name="apiVersion")
    def api_version(self) -> Optional[_builtins.str]:
        """
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        """
        return pulumi.get(self, "api_version")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def metadata(self) -> Optional['_meta.v1.outputs.ObjectMeta']:
        """
        Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
        """
        return pulumi.get(self, "metadata")

    @_builtins.property
    @pulumi.getter
    def spec(self) -> Optional['outputs.XListenerSetSpec']:
        return pulumi.get(self, "spec")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional['outputs.XListenerSetStatus']:
        return pulumi.get(self, "status")


@pulumi.output_type
class XListenerSetSpec(dict):
    """
    Spec defines the desired state of ListenerSet.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "parentRef":
            suggest = "parent_ref"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpec. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpec.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpec.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 listeners: Optional[Sequence['outputs.XListenerSetSpecListeners']] = None,
                 parent_ref: Optional['outputs.XListenerSetSpecParentRef'] = None):
        """
        Spec defines the desired state of ListenerSet.
        :param Sequence['XListenerSetSpecListenersArgs'] listeners: Listeners associated with this ListenerSet. Listeners define
               logical endpoints that are bound on this referenced parent Gateway's addresses.
               
               Listeners in a `Gateway` and their attached `ListenerSets` are concatenated
               as a list when programming the underlying infrastructure. Each listener
               name does not need to be unique across the Gateway and ListenerSets.
               See ListenerEntry.Name for more details.
               
               Implementations MUST treat the parent Gateway as having the merged
               list of all listeners from itself and attached ListenerSets using
               the following precedence:
               
               1. "parent" Gateway
               2. ListenerSet ordered by creation time (oldest first)
               3. ListenerSet ordered alphabetically by "{namespace}/{name}".
               
               An implementation MAY reject listeners by setting the ListenerEntryStatus
               `Accepted` condition to False with the Reason `TooManyListeners`
               
               If a listener has a conflict, this will be reported in the
               Status.ListenerEntryStatus setting the `Conflicted` condition to True.
               
               Implementations SHOULD be cautious about what information from the
               parent or siblings are reported to avoid accidentally leaking
               sensitive information that the child would not otherwise have access
               to. This can include contents of secrets etc.
        """
        if listeners is not None:
            pulumi.set(__self__, "listeners", listeners)
        if parent_ref is not None:
            pulumi.set(__self__, "parent_ref", parent_ref)

    @_builtins.property
    @pulumi.getter
    def listeners(self) -> Optional[Sequence['outputs.XListenerSetSpecListeners']]:
        """
        Listeners associated with this ListenerSet. Listeners define
        logical endpoints that are bound on this referenced parent Gateway's addresses.

        Listeners in a `Gateway` and their attached `ListenerSets` are concatenated
        as a list when programming the underlying infrastructure. Each listener
        name does not need to be unique across the Gateway and ListenerSets.
        See ListenerEntry.Name for more details.

        Implementations MUST treat the parent Gateway as having the merged
        list of all listeners from itself and attached ListenerSets using
        the following precedence:

        1. "parent" Gateway
        2. ListenerSet ordered by creation time (oldest first)
        3. ListenerSet ordered alphabetically by "{namespace}/{name}".

        An implementation MAY reject listeners by setting the ListenerEntryStatus
        `Accepted` condition to False with the Reason `TooManyListeners`

        If a listener has a conflict, this will be reported in the
        Status.ListenerEntryStatus setting the `Conflicted` condition to True.

        Implementations SHOULD be cautious about what information from the
        parent or siblings are reported to avoid accidentally leaking
        sensitive information that the child would not otherwise have access
        to. This can include contents of secrets etc.
        """
        return pulumi.get(self, "listeners")

    @_builtins.property
    @pulumi.getter(name="parentRef")
    def parent_ref(self) -> Optional['outputs.XListenerSetSpecParentRef']:
        return pulumi.get(self, "parent_ref")


@pulumi.output_type
class XListenerSetSpecListeners(dict):
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "allowedRoutes":
            suggest = "allowed_routes"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecListeners. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecListeners.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecListeners.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 allowed_routes: Optional['outputs.XListenerSetSpecListenersAllowedRoutes'] = None,
                 hostname: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 port: Optional[_builtins.int] = None,
                 protocol: Optional[_builtins.str] = None,
                 tls: Optional['outputs.XListenerSetSpecListenersTls'] = None):
        """
        :param _builtins.str hostname: Hostname specifies the virtual hostname to match for protocol types that
               define this concept. When unspecified, all hostnames are matched. This
               field is ignored for protocols that don't require hostname based
               matching.
               
               Implementations MUST apply Hostname matching appropriately for each of
               the following protocols:
               
               * TLS: The Listener Hostname MUST match the SNI.
               * HTTP: The Listener Hostname MUST match the Host header of the request.
               * HTTPS: The Listener Hostname SHOULD match at both the TLS and HTTP
                 protocol layers as described above. If an implementation does not
                 ensure that both the SNI and Host header match the Listener hostname,
                 it MUST clearly document that.
               
               For HTTPRoute and TLSRoute resources, there is an interaction with the
               `spec.hostnames` array. When both listener and route specify hostnames,
               there MUST be an intersection between the values for a Route to be
               accepted. For more information, refer to the Route specific Hostnames
               documentation.
               
               Hostnames that are prefixed with a wildcard label (`*.`) are interpreted
               as a suffix match. That means that a match for `*.example.com` would match
               both `test.example.com`, and `foo.test.example.com`, but not `example.com`.
        :param _builtins.str name: Name is the name of the Listener. This name MUST be unique within a
               ListenerSet.
               
               Name is not required to be unique across a Gateway and ListenerSets.
               Routes can attach to a Listener by having a ListenerSet as a parentRef
               and setting the SectionName
        :param _builtins.int port: Port is the network port. Multiple listeners may use the
               same port, subject to the Listener compatibility rules.
               
               If the port is not set or specified as zero, the implementation will assign
               a unique port. If the implementation does not support dynamic port
               assignment, it MUST set `Accepted` condition to `False` with the
               `UnsupportedPort` reason.
        :param _builtins.str protocol: Protocol specifies the network protocol this listener expects to receive.
        """
        if allowed_routes is not None:
            pulumi.set(__self__, "allowed_routes", allowed_routes)
        if hostname is not None:
            pulumi.set(__self__, "hostname", hostname)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if port is not None:
            pulumi.set(__self__, "port", port)
        if protocol is not None:
            pulumi.set(__self__, "protocol", protocol)
        if tls is not None:
            pulumi.set(__self__, "tls", tls)

    @_builtins.property
    @pulumi.getter(name="allowedRoutes")
    def allowed_routes(self) -> Optional['outputs.XListenerSetSpecListenersAllowedRoutes']:
        return pulumi.get(self, "allowed_routes")

    @_builtins.property
    @pulumi.getter
    def hostname(self) -> Optional[_builtins.str]:
        """
        Hostname specifies the virtual hostname to match for protocol types that
        define this concept. When unspecified, all hostnames are matched. This
        field is ignored for protocols that don't require hostname based
        matching.

        Implementations MUST apply Hostname matching appropriately for each of
        the following protocols:

        * TLS: The Listener Hostname MUST match the SNI.
        * HTTP: The Listener Hostname MUST match the Host header of the request.
        * HTTPS: The Listener Hostname SHOULD match at both the TLS and HTTP
          protocol layers as described above. If an implementation does not
          ensure that both the SNI and Host header match the Listener hostname,
          it MUST clearly document that.

        For HTTPRoute and TLSRoute resources, there is an interaction with the
        `spec.hostnames` array. When both listener and route specify hostnames,
        there MUST be an intersection between the values for a Route to be
        accepted. For more information, refer to the Route specific Hostnames
        documentation.

        Hostnames that are prefixed with a wildcard label (`*.`) are interpreted
        as a suffix match. That means that a match for `*.example.com` would match
        both `test.example.com`, and `foo.test.example.com`, but not `example.com`.
        """
        return pulumi.get(self, "hostname")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the Listener. This name MUST be unique within a
        ListenerSet.

        Name is not required to be unique across a Gateway and ListenerSets.
        Routes can attach to a Listener by having a ListenerSet as a parentRef
        and setting the SectionName
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def port(self) -> Optional[_builtins.int]:
        """
        Port is the network port. Multiple listeners may use the
        same port, subject to the Listener compatibility rules.

        If the port is not set or specified as zero, the implementation will assign
        a unique port. If the implementation does not support dynamic port
        assignment, it MUST set `Accepted` condition to `False` with the
        `UnsupportedPort` reason.
        """
        return pulumi.get(self, "port")

    @_builtins.property
    @pulumi.getter
    def protocol(self) -> Optional[_builtins.str]:
        """
        Protocol specifies the network protocol this listener expects to receive.
        """
        return pulumi.get(self, "protocol")

    @_builtins.property
    @pulumi.getter
    def tls(self) -> Optional['outputs.XListenerSetSpecListenersTls']:
        return pulumi.get(self, "tls")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutes(dict):
    """
    AllowedRoutes defines the types of routes that MAY be attached to a
    Listener and the trusted namespaces where those Route resources MAY be
    present.

    Although a client request may match multiple route rules, only one rule
    may ultimately receive the request. Matching precedence MUST be
    determined in order of the following criteria:

    * The most specific match as defined by the Route type.
    * The oldest Route based on creation timestamp. For example, a Route with
      a creation timestamp of "2020-09-08 01:02:03" is given precedence over
      a Route with a creation timestamp of "2020-09-08 01:02:04".
    * If everything else is equivalent, the Route appearing first in
      alphabetical order (namespace/name) should be given precedence. For
      example, foo/bar is given precedence over foo/baz.

    All valid rules within a Route attached to this Listener should be
    implemented. Invalid Route rules can be ignored (sometimes that will mean
    the full Route). If a Route rule transitions from valid to invalid,
    support for that Route rule should be dropped to ensure consistency. For
    example, even if a filter specified by a Route rule is invalid, the rest
    of the rules within that Route should still be supported.
    """
    def __init__(__self__, *,
                 kinds: Optional[Sequence['outputs.XListenerSetSpecListenersAllowedRoutesKinds']] = None,
                 namespaces: Optional['outputs.XListenerSetSpecListenersAllowedRoutesNamespaces'] = None):
        """
        AllowedRoutes defines the types of routes that MAY be attached to a
        Listener and the trusted namespaces where those Route resources MAY be
        present.

        Although a client request may match multiple route rules, only one rule
        may ultimately receive the request. Matching precedence MUST be
        determined in order of the following criteria:

        * The most specific match as defined by the Route type.
        * The oldest Route based on creation timestamp. For example, a Route with
          a creation timestamp of "2020-09-08 01:02:03" is given precedence over
          a Route with a creation timestamp of "2020-09-08 01:02:04".
        * If everything else is equivalent, the Route appearing first in
          alphabetical order (namespace/name) should be given precedence. For
          example, foo/bar is given precedence over foo/baz.

        All valid rules within a Route attached to this Listener should be
        implemented. Invalid Route rules can be ignored (sometimes that will mean
        the full Route). If a Route rule transitions from valid to invalid,
        support for that Route rule should be dropped to ensure consistency. For
        example, even if a filter specified by a Route rule is invalid, the rest
        of the rules within that Route should still be supported.
        :param Sequence['XListenerSetSpecListenersAllowedRoutesKindsArgs'] kinds: Kinds specifies the groups and kinds of Routes that are allowed to bind
               to this Gateway Listener. When unspecified or empty, the kinds of Routes
               selected are determined using the Listener protocol.
               
               A RouteGroupKind MUST correspond to kinds of Routes that are compatible
               with the application protocol specified in the Listener's Protocol field.
               If an implementation does not support or recognize this resource type, it
               MUST set the "ResolvedRefs" condition to False for this Listener with the
               "InvalidRouteKinds" reason.
               
               Support: Core
        """
        if kinds is not None:
            pulumi.set(__self__, "kinds", kinds)
        if namespaces is not None:
            pulumi.set(__self__, "namespaces", namespaces)

    @_builtins.property
    @pulumi.getter
    def kinds(self) -> Optional[Sequence['outputs.XListenerSetSpecListenersAllowedRoutesKinds']]:
        """
        Kinds specifies the groups and kinds of Routes that are allowed to bind
        to this Gateway Listener. When unspecified or empty, the kinds of Routes
        selected are determined using the Listener protocol.

        A RouteGroupKind MUST correspond to kinds of Routes that are compatible
        with the application protocol specified in the Listener's Protocol field.
        If an implementation does not support or recognize this resource type, it
        MUST set the "ResolvedRefs" condition to False for this Listener with the
        "InvalidRouteKinds" reason.

        Support: Core
        """
        return pulumi.get(self, "kinds")

    @_builtins.property
    @pulumi.getter
    def namespaces(self) -> Optional['outputs.XListenerSetSpecListenersAllowedRoutesNamespaces']:
        return pulumi.get(self, "namespaces")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesKinds(dict):
    """
    RouteGroupKind indicates the group and kind of a Route resource.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None):
        """
        RouteGroupKind indicates the group and kind of a Route resource.
        :param _builtins.str group: Group is the group of the Route.
        :param _builtins.str kind: Kind is the kind of the Route.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the Route.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is the kind of the Route.
        """
        return pulumi.get(self, "kind")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesKindsPatch(dict):
    """
    RouteGroupKind indicates the group and kind of a Route resource.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None):
        """
        RouteGroupKind indicates the group and kind of a Route resource.
        :param _builtins.str group: Group is the group of the Route.
        :param _builtins.str kind: Kind is the kind of the Route.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the Route.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is the kind of the Route.
        """
        return pulumi.get(self, "kind")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesNamespaces(dict):
    """
    Namespaces indicates namespaces from which Routes may be attached to this
    Listener. This is restricted to the namespace of this Gateway by default.

    Support: Core
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "from":
            suggest = "from_"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecListenersAllowedRoutesNamespaces. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecListenersAllowedRoutesNamespaces.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecListenersAllowedRoutesNamespaces.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 from_: Optional[_builtins.str] = None,
                 selector: Optional['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesSelector'] = None):
        """
        Namespaces indicates namespaces from which Routes may be attached to this
        Listener. This is restricted to the namespace of this Gateway by default.

        Support: Core
        :param _builtins.str from_: From indicates where Routes will be selected for this Gateway. Possible
               values are:
               
               * All: Routes in all namespaces may be used by this Gateway.
               * Selector: Routes in namespaces selected by the selector may be used by
                 this Gateway.
               * Same: Only Routes in the same namespace may be used by this Gateway.
               
               Support: Core
        """
        if from_ is not None:
            pulumi.set(__self__, "from_", from_)
        if selector is not None:
            pulumi.set(__self__, "selector", selector)

    @_builtins.property
    @pulumi.getter(name="from")
    def from_(self) -> Optional[_builtins.str]:
        """
        From indicates where Routes will be selected for this Gateway. Possible
        values are:

        * All: Routes in all namespaces may be used by this Gateway.
        * Selector: Routes in namespaces selected by the selector may be used by
          this Gateway.
        * Same: Only Routes in the same namespace may be used by this Gateway.

        Support: Core
        """
        return pulumi.get(self, "from_")

    @_builtins.property
    @pulumi.getter
    def selector(self) -> Optional['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesSelector']:
        return pulumi.get(self, "selector")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesNamespacesPatch(dict):
    """
    Namespaces indicates namespaces from which Routes may be attached to this
    Listener. This is restricted to the namespace of this Gateway by default.

    Support: Core
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "from":
            suggest = "from_"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecListenersAllowedRoutesNamespacesPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecListenersAllowedRoutesNamespacesPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecListenersAllowedRoutesNamespacesPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 from_: Optional[_builtins.str] = None,
                 selector: Optional['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesSelectorPatch'] = None):
        """
        Namespaces indicates namespaces from which Routes may be attached to this
        Listener. This is restricted to the namespace of this Gateway by default.

        Support: Core
        :param _builtins.str from_: From indicates where Routes will be selected for this Gateway. Possible
               values are:
               
               * All: Routes in all namespaces may be used by this Gateway.
               * Selector: Routes in namespaces selected by the selector may be used by
                 this Gateway.
               * Same: Only Routes in the same namespace may be used by this Gateway.
               
               Support: Core
        """
        if from_ is not None:
            pulumi.set(__self__, "from_", from_)
        if selector is not None:
            pulumi.set(__self__, "selector", selector)

    @_builtins.property
    @pulumi.getter(name="from")
    def from_(self) -> Optional[_builtins.str]:
        """
        From indicates where Routes will be selected for this Gateway. Possible
        values are:

        * All: Routes in all namespaces may be used by this Gateway.
        * Selector: Routes in namespaces selected by the selector may be used by
          this Gateway.
        * Same: Only Routes in the same namespace may be used by this Gateway.

        Support: Core
        """
        return pulumi.get(self, "from_")

    @_builtins.property
    @pulumi.getter
    def selector(self) -> Optional['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesSelectorPatch']:
        return pulumi.get(self, "selector")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesNamespacesSelector(dict):
    """
    Selector must be specified when From is set to "Selector". In that case,
    only Routes in Namespaces matching this Selector will be selected by this
    Gateway. This field is ignored for other values of "From".

    Support: Core
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "matchExpressions":
            suggest = "match_expressions"
        elif key == "matchLabels":
            suggest = "match_labels"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecListenersAllowedRoutesNamespacesSelector. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecListenersAllowedRoutesNamespacesSelector.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecListenersAllowedRoutesNamespacesSelector.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 match_expressions: Optional[Sequence['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressions']] = None,
                 match_labels: Optional[Mapping[str, _builtins.str]] = None):
        """
        Selector must be specified when From is set to "Selector". In that case,
        only Routes in Namespaces matching this Selector will be selected by this
        Gateway. This field is ignored for other values of "From".

        Support: Core
        :param Sequence['XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressionsArgs'] match_expressions: matchExpressions is a list of label selector requirements. The requirements are ANDed.
        :param Mapping[str, _builtins.str] match_labels: matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
               map is equivalent to an element of matchExpressions, whose key field is "key", the
               operator is "In", and the values array contains only "value". The requirements are ANDed.
        """
        if match_expressions is not None:
            pulumi.set(__self__, "match_expressions", match_expressions)
        if match_labels is not None:
            pulumi.set(__self__, "match_labels", match_labels)

    @_builtins.property
    @pulumi.getter(name="matchExpressions")
    def match_expressions(self) -> Optional[Sequence['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressions']]:
        """
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
        """
        return pulumi.get(self, "match_expressions")

    @_builtins.property
    @pulumi.getter(name="matchLabels")
    def match_labels(self) -> Optional[Mapping[str, _builtins.str]]:
        """
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
        map is equivalent to an element of matchExpressions, whose key field is "key", the
        operator is "In", and the values array contains only "value". The requirements are ANDed.
        """
        return pulumi.get(self, "match_labels")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressions(dict):
    """
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.
    """
    def __init__(__self__, *,
                 key: Optional[_builtins.str] = None,
                 operator: Optional[_builtins.str] = None,
                 values: Optional[Sequence[_builtins.str]] = None):
        """
        A label selector requirement is a selector that contains values, a key, and an operator that
        relates the key and values.
        :param _builtins.str key: key is the label key that the selector applies to.
        :param _builtins.str operator: operator represents a key's relationship to a set of values.
               Valid operators are In, NotIn, Exists and DoesNotExist.
        :param Sequence[_builtins.str] values: values is an array of string values. If the operator is In or NotIn,
               the values array must be non-empty. If the operator is Exists or DoesNotExist,
               the values array must be empty. This array is replaced during a strategic
               merge patch.
        """
        if key is not None:
            pulumi.set(__self__, "key", key)
        if operator is not None:
            pulumi.set(__self__, "operator", operator)
        if values is not None:
            pulumi.set(__self__, "values", values)

    @_builtins.property
    @pulumi.getter
    def key(self) -> Optional[_builtins.str]:
        """
        key is the label key that the selector applies to.
        """
        return pulumi.get(self, "key")

    @_builtins.property
    @pulumi.getter
    def operator(self) -> Optional[_builtins.str]:
        """
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
        """
        return pulumi.get(self, "operator")

    @_builtins.property
    @pulumi.getter
    def values(self) -> Optional[Sequence[_builtins.str]]:
        """
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
        """
        return pulumi.get(self, "values")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressionsPatch(dict):
    """
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.
    """
    def __init__(__self__, *,
                 key: Optional[_builtins.str] = None,
                 operator: Optional[_builtins.str] = None,
                 values: Optional[Sequence[_builtins.str]] = None):
        """
        A label selector requirement is a selector that contains values, a key, and an operator that
        relates the key and values.
        :param _builtins.str key: key is the label key that the selector applies to.
        :param _builtins.str operator: operator represents a key's relationship to a set of values.
               Valid operators are In, NotIn, Exists and DoesNotExist.
        :param Sequence[_builtins.str] values: values is an array of string values. If the operator is In or NotIn,
               the values array must be non-empty. If the operator is Exists or DoesNotExist,
               the values array must be empty. This array is replaced during a strategic
               merge patch.
        """
        if key is not None:
            pulumi.set(__self__, "key", key)
        if operator is not None:
            pulumi.set(__self__, "operator", operator)
        if values is not None:
            pulumi.set(__self__, "values", values)

    @_builtins.property
    @pulumi.getter
    def key(self) -> Optional[_builtins.str]:
        """
        key is the label key that the selector applies to.
        """
        return pulumi.get(self, "key")

    @_builtins.property
    @pulumi.getter
    def operator(self) -> Optional[_builtins.str]:
        """
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
        """
        return pulumi.get(self, "operator")

    @_builtins.property
    @pulumi.getter
    def values(self) -> Optional[Sequence[_builtins.str]]:
        """
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
        """
        return pulumi.get(self, "values")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesNamespacesSelectorPatch(dict):
    """
    Selector must be specified when From is set to "Selector". In that case,
    only Routes in Namespaces matching this Selector will be selected by this
    Gateway. This field is ignored for other values of "From".

    Support: Core
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "matchExpressions":
            suggest = "match_expressions"
        elif key == "matchLabels":
            suggest = "match_labels"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecListenersAllowedRoutesNamespacesSelectorPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecListenersAllowedRoutesNamespacesSelectorPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecListenersAllowedRoutesNamespacesSelectorPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 match_expressions: Optional[Sequence['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressionsPatch']] = None,
                 match_labels: Optional[Mapping[str, _builtins.str]] = None):
        """
        Selector must be specified when From is set to "Selector". In that case,
        only Routes in Namespaces matching this Selector will be selected by this
        Gateway. This field is ignored for other values of "From".

        Support: Core
        :param Sequence['XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressionsPatchArgs'] match_expressions: matchExpressions is a list of label selector requirements. The requirements are ANDed.
        :param Mapping[str, _builtins.str] match_labels: matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
               map is equivalent to an element of matchExpressions, whose key field is "key", the
               operator is "In", and the values array contains only "value". The requirements are ANDed.
        """
        if match_expressions is not None:
            pulumi.set(__self__, "match_expressions", match_expressions)
        if match_labels is not None:
            pulumi.set(__self__, "match_labels", match_labels)

    @_builtins.property
    @pulumi.getter(name="matchExpressions")
    def match_expressions(self) -> Optional[Sequence['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesSelectorMatchExpressionsPatch']]:
        """
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
        """
        return pulumi.get(self, "match_expressions")

    @_builtins.property
    @pulumi.getter(name="matchLabels")
    def match_labels(self) -> Optional[Mapping[str, _builtins.str]]:
        """
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
        map is equivalent to an element of matchExpressions, whose key field is "key", the
        operator is "In", and the values array contains only "value". The requirements are ANDed.
        """
        return pulumi.get(self, "match_labels")


@pulumi.output_type
class XListenerSetSpecListenersAllowedRoutesPatch(dict):
    """
    AllowedRoutes defines the types of routes that MAY be attached to a
    Listener and the trusted namespaces where those Route resources MAY be
    present.

    Although a client request may match multiple route rules, only one rule
    may ultimately receive the request. Matching precedence MUST be
    determined in order of the following criteria:

    * The most specific match as defined by the Route type.
    * The oldest Route based on creation timestamp. For example, a Route with
      a creation timestamp of "2020-09-08 01:02:03" is given precedence over
      a Route with a creation timestamp of "2020-09-08 01:02:04".
    * If everything else is equivalent, the Route appearing first in
      alphabetical order (namespace/name) should be given precedence. For
      example, foo/bar is given precedence over foo/baz.

    All valid rules within a Route attached to this Listener should be
    implemented. Invalid Route rules can be ignored (sometimes that will mean
    the full Route). If a Route rule transitions from valid to invalid,
    support for that Route rule should be dropped to ensure consistency. For
    example, even if a filter specified by a Route rule is invalid, the rest
    of the rules within that Route should still be supported.
    """
    def __init__(__self__, *,
                 kinds: Optional[Sequence['outputs.XListenerSetSpecListenersAllowedRoutesKindsPatch']] = None,
                 namespaces: Optional['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesPatch'] = None):
        """
        AllowedRoutes defines the types of routes that MAY be attached to a
        Listener and the trusted namespaces where those Route resources MAY be
        present.

        Although a client request may match multiple route rules, only one rule
        may ultimately receive the request. Matching precedence MUST be
        determined in order of the following criteria:

        * The most specific match as defined by the Route type.
        * The oldest Route based on creation timestamp. For example, a Route with
          a creation timestamp of "2020-09-08 01:02:03" is given precedence over
          a Route with a creation timestamp of "2020-09-08 01:02:04".
        * If everything else is equivalent, the Route appearing first in
          alphabetical order (namespace/name) should be given precedence. For
          example, foo/bar is given precedence over foo/baz.

        All valid rules within a Route attached to this Listener should be
        implemented. Invalid Route rules can be ignored (sometimes that will mean
        the full Route). If a Route rule transitions from valid to invalid,
        support for that Route rule should be dropped to ensure consistency. For
        example, even if a filter specified by a Route rule is invalid, the rest
        of the rules within that Route should still be supported.
        :param Sequence['XListenerSetSpecListenersAllowedRoutesKindsPatchArgs'] kinds: Kinds specifies the groups and kinds of Routes that are allowed to bind
               to this Gateway Listener. When unspecified or empty, the kinds of Routes
               selected are determined using the Listener protocol.
               
               A RouteGroupKind MUST correspond to kinds of Routes that are compatible
               with the application protocol specified in the Listener's Protocol field.
               If an implementation does not support or recognize this resource type, it
               MUST set the "ResolvedRefs" condition to False for this Listener with the
               "InvalidRouteKinds" reason.
               
               Support: Core
        """
        if kinds is not None:
            pulumi.set(__self__, "kinds", kinds)
        if namespaces is not None:
            pulumi.set(__self__, "namespaces", namespaces)

    @_builtins.property
    @pulumi.getter
    def kinds(self) -> Optional[Sequence['outputs.XListenerSetSpecListenersAllowedRoutesKindsPatch']]:
        """
        Kinds specifies the groups and kinds of Routes that are allowed to bind
        to this Gateway Listener. When unspecified or empty, the kinds of Routes
        selected are determined using the Listener protocol.

        A RouteGroupKind MUST correspond to kinds of Routes that are compatible
        with the application protocol specified in the Listener's Protocol field.
        If an implementation does not support or recognize this resource type, it
        MUST set the "ResolvedRefs" condition to False for this Listener with the
        "InvalidRouteKinds" reason.

        Support: Core
        """
        return pulumi.get(self, "kinds")

    @_builtins.property
    @pulumi.getter
    def namespaces(self) -> Optional['outputs.XListenerSetSpecListenersAllowedRoutesNamespacesPatch']:
        return pulumi.get(self, "namespaces")


@pulumi.output_type
class XListenerSetSpecListenersPatch(dict):
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "allowedRoutes":
            suggest = "allowed_routes"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecListenersPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecListenersPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecListenersPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 allowed_routes: Optional['outputs.XListenerSetSpecListenersAllowedRoutesPatch'] = None,
                 hostname: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 port: Optional[_builtins.int] = None,
                 protocol: Optional[_builtins.str] = None,
                 tls: Optional['outputs.XListenerSetSpecListenersTlsPatch'] = None):
        """
        :param _builtins.str hostname: Hostname specifies the virtual hostname to match for protocol types that
               define this concept. When unspecified, all hostnames are matched. This
               field is ignored for protocols that don't require hostname based
               matching.
               
               Implementations MUST apply Hostname matching appropriately for each of
               the following protocols:
               
               * TLS: The Listener Hostname MUST match the SNI.
               * HTTP: The Listener Hostname MUST match the Host header of the request.
               * HTTPS: The Listener Hostname SHOULD match at both the TLS and HTTP
                 protocol layers as described above. If an implementation does not
                 ensure that both the SNI and Host header match the Listener hostname,
                 it MUST clearly document that.
               
               For HTTPRoute and TLSRoute resources, there is an interaction with the
               `spec.hostnames` array. When both listener and route specify hostnames,
               there MUST be an intersection between the values for a Route to be
               accepted. For more information, refer to the Route specific Hostnames
               documentation.
               
               Hostnames that are prefixed with a wildcard label (`*.`) are interpreted
               as a suffix match. That means that a match for `*.example.com` would match
               both `test.example.com`, and `foo.test.example.com`, but not `example.com`.
        :param _builtins.str name: Name is the name of the Listener. This name MUST be unique within a
               ListenerSet.
               
               Name is not required to be unique across a Gateway and ListenerSets.
               Routes can attach to a Listener by having a ListenerSet as a parentRef
               and setting the SectionName
        :param _builtins.int port: Port is the network port. Multiple listeners may use the
               same port, subject to the Listener compatibility rules.
               
               If the port is not set or specified as zero, the implementation will assign
               a unique port. If the implementation does not support dynamic port
               assignment, it MUST set `Accepted` condition to `False` with the
               `UnsupportedPort` reason.
        :param _builtins.str protocol: Protocol specifies the network protocol this listener expects to receive.
        """
        if allowed_routes is not None:
            pulumi.set(__self__, "allowed_routes", allowed_routes)
        if hostname is not None:
            pulumi.set(__self__, "hostname", hostname)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if port is not None:
            pulumi.set(__self__, "port", port)
        if protocol is not None:
            pulumi.set(__self__, "protocol", protocol)
        if tls is not None:
            pulumi.set(__self__, "tls", tls)

    @_builtins.property
    @pulumi.getter(name="allowedRoutes")
    def allowed_routes(self) -> Optional['outputs.XListenerSetSpecListenersAllowedRoutesPatch']:
        return pulumi.get(self, "allowed_routes")

    @_builtins.property
    @pulumi.getter
    def hostname(self) -> Optional[_builtins.str]:
        """
        Hostname specifies the virtual hostname to match for protocol types that
        define this concept. When unspecified, all hostnames are matched. This
        field is ignored for protocols that don't require hostname based
        matching.

        Implementations MUST apply Hostname matching appropriately for each of
        the following protocols:

        * TLS: The Listener Hostname MUST match the SNI.
        * HTTP: The Listener Hostname MUST match the Host header of the request.
        * HTTPS: The Listener Hostname SHOULD match at both the TLS and HTTP
          protocol layers as described above. If an implementation does not
          ensure that both the SNI and Host header match the Listener hostname,
          it MUST clearly document that.

        For HTTPRoute and TLSRoute resources, there is an interaction with the
        `spec.hostnames` array. When both listener and route specify hostnames,
        there MUST be an intersection between the values for a Route to be
        accepted. For more information, refer to the Route specific Hostnames
        documentation.

        Hostnames that are prefixed with a wildcard label (`*.`) are interpreted
        as a suffix match. That means that a match for `*.example.com` would match
        both `test.example.com`, and `foo.test.example.com`, but not `example.com`.
        """
        return pulumi.get(self, "hostname")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the Listener. This name MUST be unique within a
        ListenerSet.

        Name is not required to be unique across a Gateway and ListenerSets.
        Routes can attach to a Listener by having a ListenerSet as a parentRef
        and setting the SectionName
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def port(self) -> Optional[_builtins.int]:
        """
        Port is the network port. Multiple listeners may use the
        same port, subject to the Listener compatibility rules.

        If the port is not set or specified as zero, the implementation will assign
        a unique port. If the implementation does not support dynamic port
        assignment, it MUST set `Accepted` condition to `False` with the
        `UnsupportedPort` reason.
        """
        return pulumi.get(self, "port")

    @_builtins.property
    @pulumi.getter
    def protocol(self) -> Optional[_builtins.str]:
        """
        Protocol specifies the network protocol this listener expects to receive.
        """
        return pulumi.get(self, "protocol")

    @_builtins.property
    @pulumi.getter
    def tls(self) -> Optional['outputs.XListenerSetSpecListenersTlsPatch']:
        return pulumi.get(self, "tls")


@pulumi.output_type
class XListenerSetSpecListenersTls(dict):
    """
    TLS is the TLS configuration for the Listener. This field is required if
    the Protocol field is "HTTPS" or "TLS". It is invalid to set this field
    if the Protocol field is "HTTP", "TCP", or "UDP".

    The association of SNIs to Certificate defined in ListenerTLSConfig is
    defined based on the Hostname field for this listener.

    The GatewayClass MUST use the longest matching SNI out of all
    available certificates for any TLS handshake.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "certificateRefs":
            suggest = "certificate_refs"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecListenersTls. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecListenersTls.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecListenersTls.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 certificate_refs: Optional[Sequence['outputs.XListenerSetSpecListenersTlsCertificateRefs']] = None,
                 mode: Optional[_builtins.str] = None,
                 options: Optional[Mapping[str, _builtins.str]] = None):
        """
        TLS is the TLS configuration for the Listener. This field is required if
        the Protocol field is "HTTPS" or "TLS". It is invalid to set this field
        if the Protocol field is "HTTP", "TCP", or "UDP".

        The association of SNIs to Certificate defined in ListenerTLSConfig is
        defined based on the Hostname field for this listener.

        The GatewayClass MUST use the longest matching SNI out of all
        available certificates for any TLS handshake.
        :param Sequence['XListenerSetSpecListenersTlsCertificateRefsArgs'] certificate_refs: CertificateRefs contains a series of references to Kubernetes objects that
               contains TLS certificates and private keys. These certificates are used to
               establish a TLS handshake for requests that match the hostname of the
               associated listener.
               
               A single CertificateRef to a Kubernetes Secret has "Core" support.
               Implementations MAY choose to support attaching multiple certificates to
               a Listener, but this behavior is implementation-specific.
               
               References to a resource in different namespace are invalid UNLESS there
               is a ReferenceGrant in the target namespace that allows the certificate
               to be attached. If a ReferenceGrant does not allow this reference, the
               "ResolvedRefs" condition MUST be set to False for this listener with the
               "RefNotPermitted" reason.
               
               This field is required to have at least one element when the mode is set
               to "Terminate" (default) and is optional otherwise.
               
               CertificateRefs can reference to standard Kubernetes resources, i.e.
               Secret, or implementation-specific custom resources.
               
               Support: Core - A single reference to a Kubernetes Secret of type kubernetes.io/tls
               
               Support: Implementation-specific (More than one reference or other resource types)
        :param _builtins.str mode: Mode defines the TLS behavior for the TLS session initiated by the client.
               There are two possible modes:
               
               - Terminate: The TLS session between the downstream client and the
                 Gateway is terminated at the Gateway. This mode requires certificates
                 to be specified in some way, such as populating the certificateRefs
                 field.
               - Passthrough: The TLS session is NOT terminated by the Gateway. This
                 implies that the Gateway can't decipher the TLS stream except for
                 the ClientHello message of the TLS protocol. The certificateRefs field
                 is ignored in this mode.
               
               Support: Core
        :param Mapping[str, _builtins.str] options: Options are a list of key/value pairs to enable extended TLS
               configuration for each implementation. For example, configuring the
               minimum TLS version or supported cipher suites.
               
               A set of common keys MAY be defined by the API in the future. To avoid
               any ambiguity, implementation-specific definitions MUST use
               domain-prefixed names, such as `example.com/my-custom-option`.
               Un-prefixed names are reserved for key names defined by Gateway API.
               
               Support: Implementation-specific
        """
        if certificate_refs is not None:
            pulumi.set(__self__, "certificate_refs", certificate_refs)
        if mode is not None:
            pulumi.set(__self__, "mode", mode)
        if options is not None:
            pulumi.set(__self__, "options", options)

    @_builtins.property
    @pulumi.getter(name="certificateRefs")
    def certificate_refs(self) -> Optional[Sequence['outputs.XListenerSetSpecListenersTlsCertificateRefs']]:
        """
        CertificateRefs contains a series of references to Kubernetes objects that
        contains TLS certificates and private keys. These certificates are used to
        establish a TLS handshake for requests that match the hostname of the
        associated listener.

        A single CertificateRef to a Kubernetes Secret has "Core" support.
        Implementations MAY choose to support attaching multiple certificates to
        a Listener, but this behavior is implementation-specific.

        References to a resource in different namespace are invalid UNLESS there
        is a ReferenceGrant in the target namespace that allows the certificate
        to be attached. If a ReferenceGrant does not allow this reference, the
        "ResolvedRefs" condition MUST be set to False for this listener with the
        "RefNotPermitted" reason.

        This field is required to have at least one element when the mode is set
        to "Terminate" (default) and is optional otherwise.

        CertificateRefs can reference to standard Kubernetes resources, i.e.
        Secret, or implementation-specific custom resources.

        Support: Core - A single reference to a Kubernetes Secret of type kubernetes.io/tls

        Support: Implementation-specific (More than one reference or other resource types)
        """
        return pulumi.get(self, "certificate_refs")

    @_builtins.property
    @pulumi.getter
    def mode(self) -> Optional[_builtins.str]:
        """
        Mode defines the TLS behavior for the TLS session initiated by the client.
        There are two possible modes:

        - Terminate: The TLS session between the downstream client and the
          Gateway is terminated at the Gateway. This mode requires certificates
          to be specified in some way, such as populating the certificateRefs
          field.
        - Passthrough: The TLS session is NOT terminated by the Gateway. This
          implies that the Gateway can't decipher the TLS stream except for
          the ClientHello message of the TLS protocol. The certificateRefs field
          is ignored in this mode.

        Support: Core
        """
        return pulumi.get(self, "mode")

    @_builtins.property
    @pulumi.getter
    def options(self) -> Optional[Mapping[str, _builtins.str]]:
        """
        Options are a list of key/value pairs to enable extended TLS
        configuration for each implementation. For example, configuring the
        minimum TLS version or supported cipher suites.

        A set of common keys MAY be defined by the API in the future. To avoid
        any ambiguity, implementation-specific definitions MUST use
        domain-prefixed names, such as `example.com/my-custom-option`.
        Un-prefixed names are reserved for key names defined by Gateway API.

        Support: Implementation-specific
        """
        return pulumi.get(self, "options")


@pulumi.output_type
class XListenerSetSpecListenersTlsCertificateRefs(dict):
    """
    SecretObjectReference identifies an API object including its namespace,
    defaulting to Secret.

    The API object must be valid in the cluster; the Group and Kind must
    be registered in the cluster for this reference to be valid.

    References to objects with invalid Group and Kind are not valid, and must
    be rejected by the implementation, with appropriate Conditions set
    on the containing object.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 namespace: Optional[_builtins.str] = None):
        """
        SecretObjectReference identifies an API object including its namespace,
        defaulting to Secret.

        The API object must be valid in the cluster; the Group and Kind must
        be registered in the cluster for this reference to be valid.

        References to objects with invalid Group and Kind are not valid, and must
        be rejected by the implementation, with appropriate Conditions set
        on the containing object.
        :param _builtins.str group: Group is the group of the referent. For example, "gateway.networking.k8s.io".
               When unspecified or empty string, core API group is inferred.
        :param _builtins.str kind: Kind is kind of the referent. For example "Secret".
        :param _builtins.str name: Name is the name of the referent.
        :param _builtins.str namespace: Namespace is the namespace of the referenced object. When unspecified, the local
               namespace is inferred.
               
               Note that when a namespace different than the local namespace is specified,
               a ReferenceGrant object is required in the referent namespace to allow that
               namespace's owner to accept the reference. See the ReferenceGrant
               documentation for details.
               
               Support: Core
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the referent. For example, "gateway.networking.k8s.io".
        When unspecified or empty string, core API group is inferred.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the referent. For example "Secret".
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the referent.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        """
        Namespace is the namespace of the referenced object. When unspecified, the local
        namespace is inferred.

        Note that when a namespace different than the local namespace is specified,
        a ReferenceGrant object is required in the referent namespace to allow that
        namespace's owner to accept the reference. See the ReferenceGrant
        documentation for details.

        Support: Core
        """
        return pulumi.get(self, "namespace")


@pulumi.output_type
class XListenerSetSpecListenersTlsCertificateRefsPatch(dict):
    """
    SecretObjectReference identifies an API object including its namespace,
    defaulting to Secret.

    The API object must be valid in the cluster; the Group and Kind must
    be registered in the cluster for this reference to be valid.

    References to objects with invalid Group and Kind are not valid, and must
    be rejected by the implementation, with appropriate Conditions set
    on the containing object.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 namespace: Optional[_builtins.str] = None):
        """
        SecretObjectReference identifies an API object including its namespace,
        defaulting to Secret.

        The API object must be valid in the cluster; the Group and Kind must
        be registered in the cluster for this reference to be valid.

        References to objects with invalid Group and Kind are not valid, and must
        be rejected by the implementation, with appropriate Conditions set
        on the containing object.
        :param _builtins.str group: Group is the group of the referent. For example, "gateway.networking.k8s.io".
               When unspecified or empty string, core API group is inferred.
        :param _builtins.str kind: Kind is kind of the referent. For example "Secret".
        :param _builtins.str name: Name is the name of the referent.
        :param _builtins.str namespace: Namespace is the namespace of the referenced object. When unspecified, the local
               namespace is inferred.
               
               Note that when a namespace different than the local namespace is specified,
               a ReferenceGrant object is required in the referent namespace to allow that
               namespace's owner to accept the reference. See the ReferenceGrant
               documentation for details.
               
               Support: Core
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the referent. For example, "gateway.networking.k8s.io".
        When unspecified or empty string, core API group is inferred.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the referent. For example "Secret".
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the referent.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        """
        Namespace is the namespace of the referenced object. When unspecified, the local
        namespace is inferred.

        Note that when a namespace different than the local namespace is specified,
        a ReferenceGrant object is required in the referent namespace to allow that
        namespace's owner to accept the reference. See the ReferenceGrant
        documentation for details.

        Support: Core
        """
        return pulumi.get(self, "namespace")


@pulumi.output_type
class XListenerSetSpecListenersTlsPatch(dict):
    """
    TLS is the TLS configuration for the Listener. This field is required if
    the Protocol field is "HTTPS" or "TLS". It is invalid to set this field
    if the Protocol field is "HTTP", "TCP", or "UDP".

    The association of SNIs to Certificate defined in ListenerTLSConfig is
    defined based on the Hostname field for this listener.

    The GatewayClass MUST use the longest matching SNI out of all
    available certificates for any TLS handshake.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "certificateRefs":
            suggest = "certificate_refs"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecListenersTlsPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecListenersTlsPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecListenersTlsPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 certificate_refs: Optional[Sequence['outputs.XListenerSetSpecListenersTlsCertificateRefsPatch']] = None,
                 mode: Optional[_builtins.str] = None,
                 options: Optional[Mapping[str, _builtins.str]] = None):
        """
        TLS is the TLS configuration for the Listener. This field is required if
        the Protocol field is "HTTPS" or "TLS". It is invalid to set this field
        if the Protocol field is "HTTP", "TCP", or "UDP".

        The association of SNIs to Certificate defined in ListenerTLSConfig is
        defined based on the Hostname field for this listener.

        The GatewayClass MUST use the longest matching SNI out of all
        available certificates for any TLS handshake.
        :param Sequence['XListenerSetSpecListenersTlsCertificateRefsPatchArgs'] certificate_refs: CertificateRefs contains a series of references to Kubernetes objects that
               contains TLS certificates and private keys. These certificates are used to
               establish a TLS handshake for requests that match the hostname of the
               associated listener.
               
               A single CertificateRef to a Kubernetes Secret has "Core" support.
               Implementations MAY choose to support attaching multiple certificates to
               a Listener, but this behavior is implementation-specific.
               
               References to a resource in different namespace are invalid UNLESS there
               is a ReferenceGrant in the target namespace that allows the certificate
               to be attached. If a ReferenceGrant does not allow this reference, the
               "ResolvedRefs" condition MUST be set to False for this listener with the
               "RefNotPermitted" reason.
               
               This field is required to have at least one element when the mode is set
               to "Terminate" (default) and is optional otherwise.
               
               CertificateRefs can reference to standard Kubernetes resources, i.e.
               Secret, or implementation-specific custom resources.
               
               Support: Core - A single reference to a Kubernetes Secret of type kubernetes.io/tls
               
               Support: Implementation-specific (More than one reference or other resource types)
        :param _builtins.str mode: Mode defines the TLS behavior for the TLS session initiated by the client.
               There are two possible modes:
               
               - Terminate: The TLS session between the downstream client and the
                 Gateway is terminated at the Gateway. This mode requires certificates
                 to be specified in some way, such as populating the certificateRefs
                 field.
               - Passthrough: The TLS session is NOT terminated by the Gateway. This
                 implies that the Gateway can't decipher the TLS stream except for
                 the ClientHello message of the TLS protocol. The certificateRefs field
                 is ignored in this mode.
               
               Support: Core
        :param Mapping[str, _builtins.str] options: Options are a list of key/value pairs to enable extended TLS
               configuration for each implementation. For example, configuring the
               minimum TLS version or supported cipher suites.
               
               A set of common keys MAY be defined by the API in the future. To avoid
               any ambiguity, implementation-specific definitions MUST use
               domain-prefixed names, such as `example.com/my-custom-option`.
               Un-prefixed names are reserved for key names defined by Gateway API.
               
               Support: Implementation-specific
        """
        if certificate_refs is not None:
            pulumi.set(__self__, "certificate_refs", certificate_refs)
        if mode is not None:
            pulumi.set(__self__, "mode", mode)
        if options is not None:
            pulumi.set(__self__, "options", options)

    @_builtins.property
    @pulumi.getter(name="certificateRefs")
    def certificate_refs(self) -> Optional[Sequence['outputs.XListenerSetSpecListenersTlsCertificateRefsPatch']]:
        """
        CertificateRefs contains a series of references to Kubernetes objects that
        contains TLS certificates and private keys. These certificates are used to
        establish a TLS handshake for requests that match the hostname of the
        associated listener.

        A single CertificateRef to a Kubernetes Secret has "Core" support.
        Implementations MAY choose to support attaching multiple certificates to
        a Listener, but this behavior is implementation-specific.

        References to a resource in different namespace are invalid UNLESS there
        is a ReferenceGrant in the target namespace that allows the certificate
        to be attached. If a ReferenceGrant does not allow this reference, the
        "ResolvedRefs" condition MUST be set to False for this listener with the
        "RefNotPermitted" reason.

        This field is required to have at least one element when the mode is set
        to "Terminate" (default) and is optional otherwise.

        CertificateRefs can reference to standard Kubernetes resources, i.e.
        Secret, or implementation-specific custom resources.

        Support: Core - A single reference to a Kubernetes Secret of type kubernetes.io/tls

        Support: Implementation-specific (More than one reference or other resource types)
        """
        return pulumi.get(self, "certificate_refs")

    @_builtins.property
    @pulumi.getter
    def mode(self) -> Optional[_builtins.str]:
        """
        Mode defines the TLS behavior for the TLS session initiated by the client.
        There are two possible modes:

        - Terminate: The TLS session between the downstream client and the
          Gateway is terminated at the Gateway. This mode requires certificates
          to be specified in some way, such as populating the certificateRefs
          field.
        - Passthrough: The TLS session is NOT terminated by the Gateway. This
          implies that the Gateway can't decipher the TLS stream except for
          the ClientHello message of the TLS protocol. The certificateRefs field
          is ignored in this mode.

        Support: Core
        """
        return pulumi.get(self, "mode")

    @_builtins.property
    @pulumi.getter
    def options(self) -> Optional[Mapping[str, _builtins.str]]:
        """
        Options are a list of key/value pairs to enable extended TLS
        configuration for each implementation. For example, configuring the
        minimum TLS version or supported cipher suites.

        A set of common keys MAY be defined by the API in the future. To avoid
        any ambiguity, implementation-specific definitions MUST use
        domain-prefixed names, such as `example.com/my-custom-option`.
        Un-prefixed names are reserved for key names defined by Gateway API.

        Support: Implementation-specific
        """
        return pulumi.get(self, "options")


@pulumi.output_type
class XListenerSetSpecParentRef(dict):
    """
    ParentRef references the Gateway that the listeners are attached to.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 namespace: Optional[_builtins.str] = None):
        """
        ParentRef references the Gateway that the listeners are attached to.
        :param _builtins.str group: Group is the group of the referent.
        :param _builtins.str kind: Kind is kind of the referent. For example "Gateway".
        :param _builtins.str name: Name is the name of the referent.
        :param _builtins.str namespace: Namespace is the namespace of the referent.  If not present,
               the namespace of the referent is assumed to be the same as
               the namespace of the referring object.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the referent.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the referent. For example "Gateway".
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the referent.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        """
        Namespace is the namespace of the referent.  If not present,
        the namespace of the referent is assumed to be the same as
        the namespace of the referring object.
        """
        return pulumi.get(self, "namespace")


@pulumi.output_type
class XListenerSetSpecParentRefPatch(dict):
    """
    ParentRef references the Gateway that the listeners are attached to.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 namespace: Optional[_builtins.str] = None):
        """
        ParentRef references the Gateway that the listeners are attached to.
        :param _builtins.str group: Group is the group of the referent.
        :param _builtins.str kind: Kind is kind of the referent. For example "Gateway".
        :param _builtins.str name: Name is the name of the referent.
        :param _builtins.str namespace: Namespace is the namespace of the referent.  If not present,
               the namespace of the referent is assumed to be the same as
               the namespace of the referring object.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the referent.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the referent. For example "Gateway".
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the referent.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        """
        Namespace is the namespace of the referent.  If not present,
        the namespace of the referent is assumed to be the same as
        the namespace of the referring object.
        """
        return pulumi.get(self, "namespace")


@pulumi.output_type
class XListenerSetSpecPatch(dict):
    """
    Spec defines the desired state of ListenerSet.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "parentRef":
            suggest = "parent_ref"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetSpecPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetSpecPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetSpecPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 listeners: Optional[Sequence['outputs.XListenerSetSpecListenersPatch']] = None,
                 parent_ref: Optional['outputs.XListenerSetSpecParentRefPatch'] = None):
        """
        Spec defines the desired state of ListenerSet.
        :param Sequence['XListenerSetSpecListenersPatchArgs'] listeners: Listeners associated with this ListenerSet. Listeners define
               logical endpoints that are bound on this referenced parent Gateway's addresses.
               
               Listeners in a `Gateway` and their attached `ListenerSets` are concatenated
               as a list when programming the underlying infrastructure. Each listener
               name does not need to be unique across the Gateway and ListenerSets.
               See ListenerEntry.Name for more details.
               
               Implementations MUST treat the parent Gateway as having the merged
               list of all listeners from itself and attached ListenerSets using
               the following precedence:
               
               1. "parent" Gateway
               2. ListenerSet ordered by creation time (oldest first)
               3. ListenerSet ordered alphabetically by "{namespace}/{name}".
               
               An implementation MAY reject listeners by setting the ListenerEntryStatus
               `Accepted` condition to False with the Reason `TooManyListeners`
               
               If a listener has a conflict, this will be reported in the
               Status.ListenerEntryStatus setting the `Conflicted` condition to True.
               
               Implementations SHOULD be cautious about what information from the
               parent or siblings are reported to avoid accidentally leaking
               sensitive information that the child would not otherwise have access
               to. This can include contents of secrets etc.
        """
        if listeners is not None:
            pulumi.set(__self__, "listeners", listeners)
        if parent_ref is not None:
            pulumi.set(__self__, "parent_ref", parent_ref)

    @_builtins.property
    @pulumi.getter
    def listeners(self) -> Optional[Sequence['outputs.XListenerSetSpecListenersPatch']]:
        """
        Listeners associated with this ListenerSet. Listeners define
        logical endpoints that are bound on this referenced parent Gateway's addresses.

        Listeners in a `Gateway` and their attached `ListenerSets` are concatenated
        as a list when programming the underlying infrastructure. Each listener
        name does not need to be unique across the Gateway and ListenerSets.
        See ListenerEntry.Name for more details.

        Implementations MUST treat the parent Gateway as having the merged
        list of all listeners from itself and attached ListenerSets using
        the following precedence:

        1. "parent" Gateway
        2. ListenerSet ordered by creation time (oldest first)
        3. ListenerSet ordered alphabetically by "{namespace}/{name}".

        An implementation MAY reject listeners by setting the ListenerEntryStatus
        `Accepted` condition to False with the Reason `TooManyListeners`

        If a listener has a conflict, this will be reported in the
        Status.ListenerEntryStatus setting the `Conflicted` condition to True.

        Implementations SHOULD be cautious about what information from the
        parent or siblings are reported to avoid accidentally leaking
        sensitive information that the child would not otherwise have access
        to. This can include contents of secrets etc.
        """
        return pulumi.get(self, "listeners")

    @_builtins.property
    @pulumi.getter(name="parentRef")
    def parent_ref(self) -> Optional['outputs.XListenerSetSpecParentRefPatch']:
        return pulumi.get(self, "parent_ref")


@pulumi.output_type
class XListenerSetStatus(dict):
    """
    Status defines the current state of ListenerSet.
    """
    def __init__(__self__, *,
                 conditions: Optional[Sequence['outputs.XListenerSetStatusConditions']] = None,
                 listeners: Optional[Sequence['outputs.XListenerSetStatusListeners']] = None):
        """
        Status defines the current state of ListenerSet.
        :param Sequence['XListenerSetStatusConditionsArgs'] conditions: Conditions describe the current conditions of the ListenerSet.
               
               Implementations MUST express ListenerSet conditions using the
               `ListenerSetConditionType` and `ListenerSetConditionReason`
               constants so that operators and tools can converge on a common
               vocabulary to describe ListenerSet state.
               
               Known condition types are:
               
               * "Accepted"
               * "Programmed"
        :param Sequence['XListenerSetStatusListenersArgs'] listeners: Listeners provide status for each unique listener port defined in the Spec.
        """
        if conditions is not None:
            pulumi.set(__self__, "conditions", conditions)
        if listeners is not None:
            pulumi.set(__self__, "listeners", listeners)

    @_builtins.property
    @pulumi.getter
    def conditions(self) -> Optional[Sequence['outputs.XListenerSetStatusConditions']]:
        """
        Conditions describe the current conditions of the ListenerSet.

        Implementations MUST express ListenerSet conditions using the
        `ListenerSetConditionType` and `ListenerSetConditionReason`
        constants so that operators and tools can converge on a common
        vocabulary to describe ListenerSet state.

        Known condition types are:

        * "Accepted"
        * "Programmed"
        """
        return pulumi.get(self, "conditions")

    @_builtins.property
    @pulumi.getter
    def listeners(self) -> Optional[Sequence['outputs.XListenerSetStatusListeners']]:
        """
        Listeners provide status for each unique listener port defined in the Spec.
        """
        return pulumi.get(self, "listeners")


@pulumi.output_type
class XListenerSetStatusConditions(dict):
    """
    Condition contains details for one aspect of the current state of this API Resource.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lastTransitionTime":
            suggest = "last_transition_time"
        elif key == "observedGeneration":
            suggest = "observed_generation"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetStatusConditions. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetStatusConditions.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetStatusConditions.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 last_transition_time: Optional[_builtins.str] = None,
                 message: Optional[_builtins.str] = None,
                 observed_generation: Optional[_builtins.int] = None,
                 reason: Optional[_builtins.str] = None,
                 status: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        Condition contains details for one aspect of the current state of this API Resource.
        :param _builtins.str last_transition_time: lastTransitionTime is the last time the condition transitioned from one status to another.
               This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        :param _builtins.str message: message is a human readable message indicating details about the transition.
               This may be an empty string.
        :param _builtins.int observed_generation: observedGeneration represents the .metadata.generation that the condition was set based upon.
               For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
               with respect to the current state of the instance.
        :param _builtins.str reason: reason contains a programmatic identifier indicating the reason for the condition's last transition.
               Producers of specific condition types may define expected values and meanings for this field,
               and whether the values are considered a guaranteed API.
               The value should be a CamelCase string.
               This field may not be empty.
        :param _builtins.str status: status of the condition, one of True, False, Unknown.
        :param _builtins.str type: type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        if last_transition_time is not None:
            pulumi.set(__self__, "last_transition_time", last_transition_time)
        if message is not None:
            pulumi.set(__self__, "message", message)
        if observed_generation is not None:
            pulumi.set(__self__, "observed_generation", observed_generation)
        if reason is not None:
            pulumi.set(__self__, "reason", reason)
        if status is not None:
            pulumi.set(__self__, "status", status)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="lastTransitionTime")
    def last_transition_time(self) -> Optional[_builtins.str]:
        """
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        """
        return pulumi.get(self, "last_transition_time")

    @_builtins.property
    @pulumi.getter
    def message(self) -> Optional[_builtins.str]:
        """
        message is a human readable message indicating details about the transition.
        This may be an empty string.
        """
        return pulumi.get(self, "message")

    @_builtins.property
    @pulumi.getter(name="observedGeneration")
    def observed_generation(self) -> Optional[_builtins.int]:
        """
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
        """
        return pulumi.get(self, "observed_generation")

    @_builtins.property
    @pulumi.getter
    def reason(self) -> Optional[_builtins.str]:
        """
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
        """
        return pulumi.get(self, "reason")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        status of the condition, one of True, False, Unknown.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XListenerSetStatusConditionsPatch(dict):
    """
    Condition contains details for one aspect of the current state of this API Resource.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lastTransitionTime":
            suggest = "last_transition_time"
        elif key == "observedGeneration":
            suggest = "observed_generation"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetStatusConditionsPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetStatusConditionsPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetStatusConditionsPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 last_transition_time: Optional[_builtins.str] = None,
                 message: Optional[_builtins.str] = None,
                 observed_generation: Optional[_builtins.int] = None,
                 reason: Optional[_builtins.str] = None,
                 status: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        Condition contains details for one aspect of the current state of this API Resource.
        :param _builtins.str last_transition_time: lastTransitionTime is the last time the condition transitioned from one status to another.
               This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        :param _builtins.str message: message is a human readable message indicating details about the transition.
               This may be an empty string.
        :param _builtins.int observed_generation: observedGeneration represents the .metadata.generation that the condition was set based upon.
               For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
               with respect to the current state of the instance.
        :param _builtins.str reason: reason contains a programmatic identifier indicating the reason for the condition's last transition.
               Producers of specific condition types may define expected values and meanings for this field,
               and whether the values are considered a guaranteed API.
               The value should be a CamelCase string.
               This field may not be empty.
        :param _builtins.str status: status of the condition, one of True, False, Unknown.
        :param _builtins.str type: type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        if last_transition_time is not None:
            pulumi.set(__self__, "last_transition_time", last_transition_time)
        if message is not None:
            pulumi.set(__self__, "message", message)
        if observed_generation is not None:
            pulumi.set(__self__, "observed_generation", observed_generation)
        if reason is not None:
            pulumi.set(__self__, "reason", reason)
        if status is not None:
            pulumi.set(__self__, "status", status)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="lastTransitionTime")
    def last_transition_time(self) -> Optional[_builtins.str]:
        """
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        """
        return pulumi.get(self, "last_transition_time")

    @_builtins.property
    @pulumi.getter
    def message(self) -> Optional[_builtins.str]:
        """
        message is a human readable message indicating details about the transition.
        This may be an empty string.
        """
        return pulumi.get(self, "message")

    @_builtins.property
    @pulumi.getter(name="observedGeneration")
    def observed_generation(self) -> Optional[_builtins.int]:
        """
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
        """
        return pulumi.get(self, "observed_generation")

    @_builtins.property
    @pulumi.getter
    def reason(self) -> Optional[_builtins.str]:
        """
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
        """
        return pulumi.get(self, "reason")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        status of the condition, one of True, False, Unknown.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XListenerSetStatusListeners(dict):
    """
    ListenerStatus is the status associated with a Listener.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "attachedRoutes":
            suggest = "attached_routes"
        elif key == "supportedKinds":
            suggest = "supported_kinds"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetStatusListeners. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetStatusListeners.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetStatusListeners.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 attached_routes: Optional[_builtins.int] = None,
                 conditions: Optional[Sequence['outputs.XListenerSetStatusListenersConditions']] = None,
                 name: Optional[_builtins.str] = None,
                 port: Optional[_builtins.int] = None,
                 supported_kinds: Optional[Sequence['outputs.XListenerSetStatusListenersSupportedKinds']] = None):
        """
        ListenerStatus is the status associated with a Listener.
        :param _builtins.int attached_routes: AttachedRoutes represents the total number of Routes that have been
               successfully attached to this Listener.
               
               Successful attachment of a Route to a Listener is based solely on the
               combination of the AllowedRoutes field on the corresponding Listener
               and the Route's ParentRefs field. A Route is successfully attached to
               a Listener when it is selected by the Listener's AllowedRoutes field
               AND the Route has a valid ParentRef selecting the whole Gateway
               resource or a specific Listener as a parent resource (more detail on
               attachment semantics can be found in the documentation on the various
               Route kinds ParentRefs fields). Listener or Route status does not impact
               successful attachment, i.e. the AttachedRoutes field count MUST be set
               for Listeners with condition Accepted: false and MUST count successfully
               attached Routes that may themselves have Accepted: false conditions.
               
               Uses for this field include troubleshooting Route attachment and
               measuring blast radius/impact of changes to a Listener.
        :param Sequence['XListenerSetStatusListenersConditionsArgs'] conditions: Conditions describe the current condition of this listener.
        :param _builtins.str name: Name is the name of the Listener that this status corresponds to.
        :param _builtins.int port: Port is the network port the listener is configured to listen on.
        :param Sequence['XListenerSetStatusListenersSupportedKindsArgs'] supported_kinds: SupportedKinds is the list indicating the Kinds supported by this
               listener. This MUST represent the kinds an implementation supports for
               that Listener configuration.
               
               If kinds are specified in Spec that are not supported, they MUST NOT
               appear in this list and an implementation MUST set the "ResolvedRefs"
               condition to "False" with the "InvalidRouteKinds" reason. If both valid
               and invalid Route kinds are specified, the implementation MUST
               reference the valid Route kinds that have been specified.
        """
        if attached_routes is not None:
            pulumi.set(__self__, "attached_routes", attached_routes)
        if conditions is not None:
            pulumi.set(__self__, "conditions", conditions)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if port is not None:
            pulumi.set(__self__, "port", port)
        if supported_kinds is not None:
            pulumi.set(__self__, "supported_kinds", supported_kinds)

    @_builtins.property
    @pulumi.getter(name="attachedRoutes")
    def attached_routes(self) -> Optional[_builtins.int]:
        """
        AttachedRoutes represents the total number of Routes that have been
        successfully attached to this Listener.

        Successful attachment of a Route to a Listener is based solely on the
        combination of the AllowedRoutes field on the corresponding Listener
        and the Route's ParentRefs field. A Route is successfully attached to
        a Listener when it is selected by the Listener's AllowedRoutes field
        AND the Route has a valid ParentRef selecting the whole Gateway
        resource or a specific Listener as a parent resource (more detail on
        attachment semantics can be found in the documentation on the various
        Route kinds ParentRefs fields). Listener or Route status does not impact
        successful attachment, i.e. the AttachedRoutes field count MUST be set
        for Listeners with condition Accepted: false and MUST count successfully
        attached Routes that may themselves have Accepted: false conditions.

        Uses for this field include troubleshooting Route attachment and
        measuring blast radius/impact of changes to a Listener.
        """
        return pulumi.get(self, "attached_routes")

    @_builtins.property
    @pulumi.getter
    def conditions(self) -> Optional[Sequence['outputs.XListenerSetStatusListenersConditions']]:
        """
        Conditions describe the current condition of this listener.
        """
        return pulumi.get(self, "conditions")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the Listener that this status corresponds to.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def port(self) -> Optional[_builtins.int]:
        """
        Port is the network port the listener is configured to listen on.
        """
        return pulumi.get(self, "port")

    @_builtins.property
    @pulumi.getter(name="supportedKinds")
    def supported_kinds(self) -> Optional[Sequence['outputs.XListenerSetStatusListenersSupportedKinds']]:
        """
        SupportedKinds is the list indicating the Kinds supported by this
        listener. This MUST represent the kinds an implementation supports for
        that Listener configuration.

        If kinds are specified in Spec that are not supported, they MUST NOT
        appear in this list and an implementation MUST set the "ResolvedRefs"
        condition to "False" with the "InvalidRouteKinds" reason. If both valid
        and invalid Route kinds are specified, the implementation MUST
        reference the valid Route kinds that have been specified.
        """
        return pulumi.get(self, "supported_kinds")


@pulumi.output_type
class XListenerSetStatusListenersConditions(dict):
    """
    Condition contains details for one aspect of the current state of this API Resource.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lastTransitionTime":
            suggest = "last_transition_time"
        elif key == "observedGeneration":
            suggest = "observed_generation"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetStatusListenersConditions. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetStatusListenersConditions.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetStatusListenersConditions.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 last_transition_time: Optional[_builtins.str] = None,
                 message: Optional[_builtins.str] = None,
                 observed_generation: Optional[_builtins.int] = None,
                 reason: Optional[_builtins.str] = None,
                 status: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        Condition contains details for one aspect of the current state of this API Resource.
        :param _builtins.str last_transition_time: lastTransitionTime is the last time the condition transitioned from one status to another.
               This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        :param _builtins.str message: message is a human readable message indicating details about the transition.
               This may be an empty string.
        :param _builtins.int observed_generation: observedGeneration represents the .metadata.generation that the condition was set based upon.
               For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
               with respect to the current state of the instance.
        :param _builtins.str reason: reason contains a programmatic identifier indicating the reason for the condition's last transition.
               Producers of specific condition types may define expected values and meanings for this field,
               and whether the values are considered a guaranteed API.
               The value should be a CamelCase string.
               This field may not be empty.
        :param _builtins.str status: status of the condition, one of True, False, Unknown.
        :param _builtins.str type: type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        if last_transition_time is not None:
            pulumi.set(__self__, "last_transition_time", last_transition_time)
        if message is not None:
            pulumi.set(__self__, "message", message)
        if observed_generation is not None:
            pulumi.set(__self__, "observed_generation", observed_generation)
        if reason is not None:
            pulumi.set(__self__, "reason", reason)
        if status is not None:
            pulumi.set(__self__, "status", status)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="lastTransitionTime")
    def last_transition_time(self) -> Optional[_builtins.str]:
        """
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        """
        return pulumi.get(self, "last_transition_time")

    @_builtins.property
    @pulumi.getter
    def message(self) -> Optional[_builtins.str]:
        """
        message is a human readable message indicating details about the transition.
        This may be an empty string.
        """
        return pulumi.get(self, "message")

    @_builtins.property
    @pulumi.getter(name="observedGeneration")
    def observed_generation(self) -> Optional[_builtins.int]:
        """
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
        """
        return pulumi.get(self, "observed_generation")

    @_builtins.property
    @pulumi.getter
    def reason(self) -> Optional[_builtins.str]:
        """
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
        """
        return pulumi.get(self, "reason")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        status of the condition, one of True, False, Unknown.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XListenerSetStatusListenersConditionsPatch(dict):
    """
    Condition contains details for one aspect of the current state of this API Resource.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lastTransitionTime":
            suggest = "last_transition_time"
        elif key == "observedGeneration":
            suggest = "observed_generation"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetStatusListenersConditionsPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetStatusListenersConditionsPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetStatusListenersConditionsPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 last_transition_time: Optional[_builtins.str] = None,
                 message: Optional[_builtins.str] = None,
                 observed_generation: Optional[_builtins.int] = None,
                 reason: Optional[_builtins.str] = None,
                 status: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        Condition contains details for one aspect of the current state of this API Resource.
        :param _builtins.str last_transition_time: lastTransitionTime is the last time the condition transitioned from one status to another.
               This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        :param _builtins.str message: message is a human readable message indicating details about the transition.
               This may be an empty string.
        :param _builtins.int observed_generation: observedGeneration represents the .metadata.generation that the condition was set based upon.
               For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
               with respect to the current state of the instance.
        :param _builtins.str reason: reason contains a programmatic identifier indicating the reason for the condition's last transition.
               Producers of specific condition types may define expected values and meanings for this field,
               and whether the values are considered a guaranteed API.
               The value should be a CamelCase string.
               This field may not be empty.
        :param _builtins.str status: status of the condition, one of True, False, Unknown.
        :param _builtins.str type: type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        if last_transition_time is not None:
            pulumi.set(__self__, "last_transition_time", last_transition_time)
        if message is not None:
            pulumi.set(__self__, "message", message)
        if observed_generation is not None:
            pulumi.set(__self__, "observed_generation", observed_generation)
        if reason is not None:
            pulumi.set(__self__, "reason", reason)
        if status is not None:
            pulumi.set(__self__, "status", status)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="lastTransitionTime")
    def last_transition_time(self) -> Optional[_builtins.str]:
        """
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        """
        return pulumi.get(self, "last_transition_time")

    @_builtins.property
    @pulumi.getter
    def message(self) -> Optional[_builtins.str]:
        """
        message is a human readable message indicating details about the transition.
        This may be an empty string.
        """
        return pulumi.get(self, "message")

    @_builtins.property
    @pulumi.getter(name="observedGeneration")
    def observed_generation(self) -> Optional[_builtins.int]:
        """
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
        """
        return pulumi.get(self, "observed_generation")

    @_builtins.property
    @pulumi.getter
    def reason(self) -> Optional[_builtins.str]:
        """
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
        """
        return pulumi.get(self, "reason")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        status of the condition, one of True, False, Unknown.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XListenerSetStatusListenersPatch(dict):
    """
    ListenerStatus is the status associated with a Listener.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "attachedRoutes":
            suggest = "attached_routes"
        elif key == "supportedKinds":
            suggest = "supported_kinds"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XListenerSetStatusListenersPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XListenerSetStatusListenersPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XListenerSetStatusListenersPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 attached_routes: Optional[_builtins.int] = None,
                 conditions: Optional[Sequence['outputs.XListenerSetStatusListenersConditionsPatch']] = None,
                 name: Optional[_builtins.str] = None,
                 port: Optional[_builtins.int] = None,
                 supported_kinds: Optional[Sequence['outputs.XListenerSetStatusListenersSupportedKindsPatch']] = None):
        """
        ListenerStatus is the status associated with a Listener.
        :param _builtins.int attached_routes: AttachedRoutes represents the total number of Routes that have been
               successfully attached to this Listener.
               
               Successful attachment of a Route to a Listener is based solely on the
               combination of the AllowedRoutes field on the corresponding Listener
               and the Route's ParentRefs field. A Route is successfully attached to
               a Listener when it is selected by the Listener's AllowedRoutes field
               AND the Route has a valid ParentRef selecting the whole Gateway
               resource or a specific Listener as a parent resource (more detail on
               attachment semantics can be found in the documentation on the various
               Route kinds ParentRefs fields). Listener or Route status does not impact
               successful attachment, i.e. the AttachedRoutes field count MUST be set
               for Listeners with condition Accepted: false and MUST count successfully
               attached Routes that may themselves have Accepted: false conditions.
               
               Uses for this field include troubleshooting Route attachment and
               measuring blast radius/impact of changes to a Listener.
        :param Sequence['XListenerSetStatusListenersConditionsPatchArgs'] conditions: Conditions describe the current condition of this listener.
        :param _builtins.str name: Name is the name of the Listener that this status corresponds to.
        :param _builtins.int port: Port is the network port the listener is configured to listen on.
        :param Sequence['XListenerSetStatusListenersSupportedKindsPatchArgs'] supported_kinds: SupportedKinds is the list indicating the Kinds supported by this
               listener. This MUST represent the kinds an implementation supports for
               that Listener configuration.
               
               If kinds are specified in Spec that are not supported, they MUST NOT
               appear in this list and an implementation MUST set the "ResolvedRefs"
               condition to "False" with the "InvalidRouteKinds" reason. If both valid
               and invalid Route kinds are specified, the implementation MUST
               reference the valid Route kinds that have been specified.
        """
        if attached_routes is not None:
            pulumi.set(__self__, "attached_routes", attached_routes)
        if conditions is not None:
            pulumi.set(__self__, "conditions", conditions)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if port is not None:
            pulumi.set(__self__, "port", port)
        if supported_kinds is not None:
            pulumi.set(__self__, "supported_kinds", supported_kinds)

    @_builtins.property
    @pulumi.getter(name="attachedRoutes")
    def attached_routes(self) -> Optional[_builtins.int]:
        """
        AttachedRoutes represents the total number of Routes that have been
        successfully attached to this Listener.

        Successful attachment of a Route to a Listener is based solely on the
        combination of the AllowedRoutes field on the corresponding Listener
        and the Route's ParentRefs field. A Route is successfully attached to
        a Listener when it is selected by the Listener's AllowedRoutes field
        AND the Route has a valid ParentRef selecting the whole Gateway
        resource or a specific Listener as a parent resource (more detail on
        attachment semantics can be found in the documentation on the various
        Route kinds ParentRefs fields). Listener or Route status does not impact
        successful attachment, i.e. the AttachedRoutes field count MUST be set
        for Listeners with condition Accepted: false and MUST count successfully
        attached Routes that may themselves have Accepted: false conditions.

        Uses for this field include troubleshooting Route attachment and
        measuring blast radius/impact of changes to a Listener.
        """
        return pulumi.get(self, "attached_routes")

    @_builtins.property
    @pulumi.getter
    def conditions(self) -> Optional[Sequence['outputs.XListenerSetStatusListenersConditionsPatch']]:
        """
        Conditions describe the current condition of this listener.
        """
        return pulumi.get(self, "conditions")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the Listener that this status corresponds to.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def port(self) -> Optional[_builtins.int]:
        """
        Port is the network port the listener is configured to listen on.
        """
        return pulumi.get(self, "port")

    @_builtins.property
    @pulumi.getter(name="supportedKinds")
    def supported_kinds(self) -> Optional[Sequence['outputs.XListenerSetStatusListenersSupportedKindsPatch']]:
        """
        SupportedKinds is the list indicating the Kinds supported by this
        listener. This MUST represent the kinds an implementation supports for
        that Listener configuration.

        If kinds are specified in Spec that are not supported, they MUST NOT
        appear in this list and an implementation MUST set the "ResolvedRefs"
        condition to "False" with the "InvalidRouteKinds" reason. If both valid
        and invalid Route kinds are specified, the implementation MUST
        reference the valid Route kinds that have been specified.
        """
        return pulumi.get(self, "supported_kinds")


@pulumi.output_type
class XListenerSetStatusListenersSupportedKinds(dict):
    """
    RouteGroupKind indicates the group and kind of a Route resource.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None):
        """
        RouteGroupKind indicates the group and kind of a Route resource.
        :param _builtins.str group: Group is the group of the Route.
        :param _builtins.str kind: Kind is the kind of the Route.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the Route.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is the kind of the Route.
        """
        return pulumi.get(self, "kind")


@pulumi.output_type
class XListenerSetStatusListenersSupportedKindsPatch(dict):
    """
    RouteGroupKind indicates the group and kind of a Route resource.
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None):
        """
        RouteGroupKind indicates the group and kind of a Route resource.
        :param _builtins.str group: Group is the group of the Route.
        :param _builtins.str kind: Kind is the kind of the Route.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the Route.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is the kind of the Route.
        """
        return pulumi.get(self, "kind")


@pulumi.output_type
class XListenerSetStatusPatch(dict):
    """
    Status defines the current state of ListenerSet.
    """
    def __init__(__self__, *,
                 conditions: Optional[Sequence['outputs.XListenerSetStatusConditionsPatch']] = None,
                 listeners: Optional[Sequence['outputs.XListenerSetStatusListenersPatch']] = None):
        """
        Status defines the current state of ListenerSet.
        :param Sequence['XListenerSetStatusConditionsPatchArgs'] conditions: Conditions describe the current conditions of the ListenerSet.
               
               Implementations MUST express ListenerSet conditions using the
               `ListenerSetConditionType` and `ListenerSetConditionReason`
               constants so that operators and tools can converge on a common
               vocabulary to describe ListenerSet state.
               
               Known condition types are:
               
               * "Accepted"
               * "Programmed"
        :param Sequence['XListenerSetStatusListenersPatchArgs'] listeners: Listeners provide status for each unique listener port defined in the Spec.
        """
        if conditions is not None:
            pulumi.set(__self__, "conditions", conditions)
        if listeners is not None:
            pulumi.set(__self__, "listeners", listeners)

    @_builtins.property
    @pulumi.getter
    def conditions(self) -> Optional[Sequence['outputs.XListenerSetStatusConditionsPatch']]:
        """
        Conditions describe the current conditions of the ListenerSet.

        Implementations MUST express ListenerSet conditions using the
        `ListenerSetConditionType` and `ListenerSetConditionReason`
        constants so that operators and tools can converge on a common
        vocabulary to describe ListenerSet state.

        Known condition types are:

        * "Accepted"
        * "Programmed"
        """
        return pulumi.get(self, "conditions")

    @_builtins.property
    @pulumi.getter
    def listeners(self) -> Optional[Sequence['outputs.XListenerSetStatusListenersPatch']]:
        """
        Listeners provide status for each unique listener port defined in the Spec.
        """
        return pulumi.get(self, "listeners")


@pulumi.output_type
class XMesh(dict):
    """
    XMesh defines mesh-wide characteristics of a GAMMA-compliant service mesh.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "apiVersion":
            suggest = "api_version"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XMesh. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XMesh.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XMesh.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 api_version: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 metadata: Optional['_meta.v1.outputs.ObjectMeta'] = None,
                 spec: Optional['outputs.XMeshSpec'] = None,
                 status: Optional['outputs.XMeshStatus'] = None):
        """
        XMesh defines mesh-wide characteristics of a GAMMA-compliant service mesh.
        :param _builtins.str api_version: APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        :param _builtins.str kind: Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        :param '_meta.v1.ObjectMetaArgs' metadata: Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
        """
        if api_version is not None:
            pulumi.set(__self__, "api_version", 'gateway.networking.x-k8s.io/v1alpha1')
        if kind is not None:
            pulumi.set(__self__, "kind", 'XMesh')
        if metadata is not None:
            pulumi.set(__self__, "metadata", metadata)
        if spec is not None:
            pulumi.set(__self__, "spec", spec)
        if status is not None:
            pulumi.set(__self__, "status", status)

    @_builtins.property
    @pulumi.getter(name="apiVersion")
    def api_version(self) -> Optional[_builtins.str]:
        """
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
        """
        return pulumi.get(self, "api_version")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def metadata(self) -> Optional['_meta.v1.outputs.ObjectMeta']:
        """
        Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
        """
        return pulumi.get(self, "metadata")

    @_builtins.property
    @pulumi.getter
    def spec(self) -> Optional['outputs.XMeshSpec']:
        return pulumi.get(self, "spec")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional['outputs.XMeshStatus']:
        return pulumi.get(self, "status")


@pulumi.output_type
class XMeshSpec(dict):
    """
    Spec defines the desired state of XMesh.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "controllerName":
            suggest = "controller_name"
        elif key == "parametersRef":
            suggest = "parameters_ref"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XMeshSpec. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XMeshSpec.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XMeshSpec.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 controller_name: Optional[_builtins.str] = None,
                 description: Optional[_builtins.str] = None,
                 parameters_ref: Optional['outputs.XMeshSpecParametersRef'] = None):
        """
        Spec defines the desired state of XMesh.
        :param _builtins.str controller_name: ControllerName is the name of a controller that is managing Gateway API
               resources for mesh traffic management. The value of this field MUST be a
               domain prefixed path.
               
               Example: "example.com/awesome-mesh".
               
               This field is not mutable and cannot be empty.
               
               Support: Core
        :param _builtins.str description: Description optionally provides a human-readable description of a Mesh.
        """
        if controller_name is not None:
            pulumi.set(__self__, "controller_name", controller_name)
        if description is not None:
            pulumi.set(__self__, "description", description)
        if parameters_ref is not None:
            pulumi.set(__self__, "parameters_ref", parameters_ref)

    @_builtins.property
    @pulumi.getter(name="controllerName")
    def controller_name(self) -> Optional[_builtins.str]:
        """
        ControllerName is the name of a controller that is managing Gateway API
        resources for mesh traffic management. The value of this field MUST be a
        domain prefixed path.

        Example: "example.com/awesome-mesh".

        This field is not mutable and cannot be empty.

        Support: Core
        """
        return pulumi.get(self, "controller_name")

    @_builtins.property
    @pulumi.getter
    def description(self) -> Optional[_builtins.str]:
        """
        Description optionally provides a human-readable description of a Mesh.
        """
        return pulumi.get(self, "description")

    @_builtins.property
    @pulumi.getter(name="parametersRef")
    def parameters_ref(self) -> Optional['outputs.XMeshSpecParametersRef']:
        return pulumi.get(self, "parameters_ref")


@pulumi.output_type
class XMeshSpecParametersRef(dict):
    """
    ParametersRef is an optional reference to a resource that contains
    implementation-specific configuration for this Mesh. If no
    implementation-specific parameters are needed, this field MUST be
    omitted.

    ParametersRef can reference a standard Kubernetes resource, i.e.
    ConfigMap, or an implementation-specific custom resource. The resource
    can be cluster-scoped or namespace-scoped.

    If the referent cannot be found, refers to an unsupported kind, or when
    the data within that resource is malformed, the Mesh MUST be rejected
    with the "Accepted" status condition set to "False" and an
    "InvalidParameters" reason.

    Support: Implementation-specific
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 namespace: Optional[_builtins.str] = None):
        """
        ParametersRef is an optional reference to a resource that contains
        implementation-specific configuration for this Mesh. If no
        implementation-specific parameters are needed, this field MUST be
        omitted.

        ParametersRef can reference a standard Kubernetes resource, i.e.
        ConfigMap, or an implementation-specific custom resource. The resource
        can be cluster-scoped or namespace-scoped.

        If the referent cannot be found, refers to an unsupported kind, or when
        the data within that resource is malformed, the Mesh MUST be rejected
        with the "Accepted" status condition set to "False" and an
        "InvalidParameters" reason.

        Support: Implementation-specific
        :param _builtins.str group: Group is the group of the referent.
        :param _builtins.str kind: Kind is kind of the referent.
        :param _builtins.str name: Name is the name of the referent.
        :param _builtins.str namespace: Namespace is the namespace of the referent.
               This field is required when referring to a Namespace-scoped resource and
               MUST be unset when referring to a Cluster-scoped resource.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the referent.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the referent.
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the referent.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        """
        Namespace is the namespace of the referent.
        This field is required when referring to a Namespace-scoped resource and
        MUST be unset when referring to a Cluster-scoped resource.
        """
        return pulumi.get(self, "namespace")


@pulumi.output_type
class XMeshSpecParametersRefPatch(dict):
    """
    ParametersRef is an optional reference to a resource that contains
    implementation-specific configuration for this Mesh. If no
    implementation-specific parameters are needed, this field MUST be
    omitted.

    ParametersRef can reference a standard Kubernetes resource, i.e.
    ConfigMap, or an implementation-specific custom resource. The resource
    can be cluster-scoped or namespace-scoped.

    If the referent cannot be found, refers to an unsupported kind, or when
    the data within that resource is malformed, the Mesh MUST be rejected
    with the "Accepted" status condition set to "False" and an
    "InvalidParameters" reason.

    Support: Implementation-specific
    """
    def __init__(__self__, *,
                 group: Optional[_builtins.str] = None,
                 kind: Optional[_builtins.str] = None,
                 name: Optional[_builtins.str] = None,
                 namespace: Optional[_builtins.str] = None):
        """
        ParametersRef is an optional reference to a resource that contains
        implementation-specific configuration for this Mesh. If no
        implementation-specific parameters are needed, this field MUST be
        omitted.

        ParametersRef can reference a standard Kubernetes resource, i.e.
        ConfigMap, or an implementation-specific custom resource. The resource
        can be cluster-scoped or namespace-scoped.

        If the referent cannot be found, refers to an unsupported kind, or when
        the data within that resource is malformed, the Mesh MUST be rejected
        with the "Accepted" status condition set to "False" and an
        "InvalidParameters" reason.

        Support: Implementation-specific
        :param _builtins.str group: Group is the group of the referent.
        :param _builtins.str kind: Kind is kind of the referent.
        :param _builtins.str name: Name is the name of the referent.
        :param _builtins.str namespace: Namespace is the namespace of the referent.
               This field is required when referring to a Namespace-scoped resource and
               MUST be unset when referring to a Cluster-scoped resource.
        """
        if group is not None:
            pulumi.set(__self__, "group", group)
        if kind is not None:
            pulumi.set(__self__, "kind", kind)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)

    @_builtins.property
    @pulumi.getter
    def group(self) -> Optional[_builtins.str]:
        """
        Group is the group of the referent.
        """
        return pulumi.get(self, "group")

    @_builtins.property
    @pulumi.getter
    def kind(self) -> Optional[_builtins.str]:
        """
        Kind is kind of the referent.
        """
        return pulumi.get(self, "kind")

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        Name is the name of the referent.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        """
        Namespace is the namespace of the referent.
        This field is required when referring to a Namespace-scoped resource and
        MUST be unset when referring to a Cluster-scoped resource.
        """
        return pulumi.get(self, "namespace")


@pulumi.output_type
class XMeshSpecPatch(dict):
    """
    Spec defines the desired state of XMesh.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "controllerName":
            suggest = "controller_name"
        elif key == "parametersRef":
            suggest = "parameters_ref"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XMeshSpecPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XMeshSpecPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XMeshSpecPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 controller_name: Optional[_builtins.str] = None,
                 description: Optional[_builtins.str] = None,
                 parameters_ref: Optional['outputs.XMeshSpecParametersRefPatch'] = None):
        """
        Spec defines the desired state of XMesh.
        :param _builtins.str controller_name: ControllerName is the name of a controller that is managing Gateway API
               resources for mesh traffic management. The value of this field MUST be a
               domain prefixed path.
               
               Example: "example.com/awesome-mesh".
               
               This field is not mutable and cannot be empty.
               
               Support: Core
        :param _builtins.str description: Description optionally provides a human-readable description of a Mesh.
        """
        if controller_name is not None:
            pulumi.set(__self__, "controller_name", controller_name)
        if description is not None:
            pulumi.set(__self__, "description", description)
        if parameters_ref is not None:
            pulumi.set(__self__, "parameters_ref", parameters_ref)

    @_builtins.property
    @pulumi.getter(name="controllerName")
    def controller_name(self) -> Optional[_builtins.str]:
        """
        ControllerName is the name of a controller that is managing Gateway API
        resources for mesh traffic management. The value of this field MUST be a
        domain prefixed path.

        Example: "example.com/awesome-mesh".

        This field is not mutable and cannot be empty.

        Support: Core
        """
        return pulumi.get(self, "controller_name")

    @_builtins.property
    @pulumi.getter
    def description(self) -> Optional[_builtins.str]:
        """
        Description optionally provides a human-readable description of a Mesh.
        """
        return pulumi.get(self, "description")

    @_builtins.property
    @pulumi.getter(name="parametersRef")
    def parameters_ref(self) -> Optional['outputs.XMeshSpecParametersRefPatch']:
        return pulumi.get(self, "parameters_ref")


@pulumi.output_type
class XMeshStatus(dict):
    """
    Status defines the current state of XMesh.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "supportedFeatures":
            suggest = "supported_features"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XMeshStatus. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XMeshStatus.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XMeshStatus.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 conditions: Optional[Sequence['outputs.XMeshStatusConditions']] = None,
                 supported_features: Optional[Sequence['outputs.XMeshStatusSupportedFeatures']] = None):
        """
        Status defines the current state of XMesh.
        :param Sequence['XMeshStatusConditionsArgs'] conditions: Conditions is the current status from the controller for
               this Mesh.
               
               Controllers should prefer to publish conditions using values
               of MeshConditionType for the type of each Condition.
        :param Sequence['XMeshStatusSupportedFeaturesArgs'] supported_features: SupportedFeatures is the set of features the Mesh support.
               It MUST be sorted in ascending alphabetical order by the Name key.
        """
        if conditions is not None:
            pulumi.set(__self__, "conditions", conditions)
        if supported_features is not None:
            pulumi.set(__self__, "supported_features", supported_features)

    @_builtins.property
    @pulumi.getter
    def conditions(self) -> Optional[Sequence['outputs.XMeshStatusConditions']]:
        """
        Conditions is the current status from the controller for
        this Mesh.

        Controllers should prefer to publish conditions using values
        of MeshConditionType for the type of each Condition.
        """
        return pulumi.get(self, "conditions")

    @_builtins.property
    @pulumi.getter(name="supportedFeatures")
    def supported_features(self) -> Optional[Sequence['outputs.XMeshStatusSupportedFeatures']]:
        """
        SupportedFeatures is the set of features the Mesh support.
        It MUST be sorted in ascending alphabetical order by the Name key.
        """
        return pulumi.get(self, "supported_features")


@pulumi.output_type
class XMeshStatusConditions(dict):
    """
    Condition contains details for one aspect of the current state of this API Resource.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lastTransitionTime":
            suggest = "last_transition_time"
        elif key == "observedGeneration":
            suggest = "observed_generation"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XMeshStatusConditions. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XMeshStatusConditions.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XMeshStatusConditions.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 last_transition_time: Optional[_builtins.str] = None,
                 message: Optional[_builtins.str] = None,
                 observed_generation: Optional[_builtins.int] = None,
                 reason: Optional[_builtins.str] = None,
                 status: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        Condition contains details for one aspect of the current state of this API Resource.
        :param _builtins.str last_transition_time: lastTransitionTime is the last time the condition transitioned from one status to another.
               This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        :param _builtins.str message: message is a human readable message indicating details about the transition.
               This may be an empty string.
        :param _builtins.int observed_generation: observedGeneration represents the .metadata.generation that the condition was set based upon.
               For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
               with respect to the current state of the instance.
        :param _builtins.str reason: reason contains a programmatic identifier indicating the reason for the condition's last transition.
               Producers of specific condition types may define expected values and meanings for this field,
               and whether the values are considered a guaranteed API.
               The value should be a CamelCase string.
               This field may not be empty.
        :param _builtins.str status: status of the condition, one of True, False, Unknown.
        :param _builtins.str type: type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        if last_transition_time is not None:
            pulumi.set(__self__, "last_transition_time", last_transition_time)
        if message is not None:
            pulumi.set(__self__, "message", message)
        if observed_generation is not None:
            pulumi.set(__self__, "observed_generation", observed_generation)
        if reason is not None:
            pulumi.set(__self__, "reason", reason)
        if status is not None:
            pulumi.set(__self__, "status", status)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="lastTransitionTime")
    def last_transition_time(self) -> Optional[_builtins.str]:
        """
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        """
        return pulumi.get(self, "last_transition_time")

    @_builtins.property
    @pulumi.getter
    def message(self) -> Optional[_builtins.str]:
        """
        message is a human readable message indicating details about the transition.
        This may be an empty string.
        """
        return pulumi.get(self, "message")

    @_builtins.property
    @pulumi.getter(name="observedGeneration")
    def observed_generation(self) -> Optional[_builtins.int]:
        """
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
        """
        return pulumi.get(self, "observed_generation")

    @_builtins.property
    @pulumi.getter
    def reason(self) -> Optional[_builtins.str]:
        """
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
        """
        return pulumi.get(self, "reason")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        status of the condition, one of True, False, Unknown.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XMeshStatusConditionsPatch(dict):
    """
    Condition contains details for one aspect of the current state of this API Resource.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "lastTransitionTime":
            suggest = "last_transition_time"
        elif key == "observedGeneration":
            suggest = "observed_generation"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XMeshStatusConditionsPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XMeshStatusConditionsPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XMeshStatusConditionsPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 last_transition_time: Optional[_builtins.str] = None,
                 message: Optional[_builtins.str] = None,
                 observed_generation: Optional[_builtins.int] = None,
                 reason: Optional[_builtins.str] = None,
                 status: Optional[_builtins.str] = None,
                 type: Optional[_builtins.str] = None):
        """
        Condition contains details for one aspect of the current state of this API Resource.
        :param _builtins.str last_transition_time: lastTransitionTime is the last time the condition transitioned from one status to another.
               This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        :param _builtins.str message: message is a human readable message indicating details about the transition.
               This may be an empty string.
        :param _builtins.int observed_generation: observedGeneration represents the .metadata.generation that the condition was set based upon.
               For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
               with respect to the current state of the instance.
        :param _builtins.str reason: reason contains a programmatic identifier indicating the reason for the condition's last transition.
               Producers of specific condition types may define expected values and meanings for this field,
               and whether the values are considered a guaranteed API.
               The value should be a CamelCase string.
               This field may not be empty.
        :param _builtins.str status: status of the condition, one of True, False, Unknown.
        :param _builtins.str type: type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        if last_transition_time is not None:
            pulumi.set(__self__, "last_transition_time", last_transition_time)
        if message is not None:
            pulumi.set(__self__, "message", message)
        if observed_generation is not None:
            pulumi.set(__self__, "observed_generation", observed_generation)
        if reason is not None:
            pulumi.set(__self__, "reason", reason)
        if status is not None:
            pulumi.set(__self__, "status", status)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter(name="lastTransitionTime")
    def last_transition_time(self) -> Optional[_builtins.str]:
        """
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
        """
        return pulumi.get(self, "last_transition_time")

    @_builtins.property
    @pulumi.getter
    def message(self) -> Optional[_builtins.str]:
        """
        message is a human readable message indicating details about the transition.
        This may be an empty string.
        """
        return pulumi.get(self, "message")

    @_builtins.property
    @pulumi.getter(name="observedGeneration")
    def observed_generation(self) -> Optional[_builtins.int]:
        """
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
        """
        return pulumi.get(self, "observed_generation")

    @_builtins.property
    @pulumi.getter
    def reason(self) -> Optional[_builtins.str]:
        """
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
        """
        return pulumi.get(self, "reason")

    @_builtins.property
    @pulumi.getter
    def status(self) -> Optional[_builtins.str]:
        """
        status of the condition, one of True, False, Unknown.
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def type(self) -> Optional[_builtins.str]:
        """
        type of condition in CamelCase or in foo.example.com/CamelCase.
        """
        return pulumi.get(self, "type")


@pulumi.output_type
class XMeshStatusPatch(dict):
    """
    Status defines the current state of XMesh.
    """
    @staticmethod
    def __key_warning(key: str):
        suggest = None
        if key == "supportedFeatures":
            suggest = "supported_features"

        if suggest:
            pulumi.log.warn(f"Key '{key}' not found in XMeshStatusPatch. Access the value via the '{suggest}' property getter instead.")

    def __getitem__(self, key: str) -> Any:
        XMeshStatusPatch.__key_warning(key)
        return super().__getitem__(key)

    def get(self, key: str, default = None) -> Any:
        XMeshStatusPatch.__key_warning(key)
        return super().get(key, default)

    def __init__(__self__, *,
                 conditions: Optional[Sequence['outputs.XMeshStatusConditionsPatch']] = None,
                 supported_features: Optional[Sequence['outputs.XMeshStatusSupportedFeaturesPatch']] = None):
        """
        Status defines the current state of XMesh.
        :param Sequence['XMeshStatusConditionsPatchArgs'] conditions: Conditions is the current status from the controller for
               this Mesh.
               
               Controllers should prefer to publish conditions using values
               of MeshConditionType for the type of each Condition.
        :param Sequence['XMeshStatusSupportedFeaturesPatchArgs'] supported_features: SupportedFeatures is the set of features the Mesh support.
               It MUST be sorted in ascending alphabetical order by the Name key.
        """
        if conditions is not None:
            pulumi.set(__self__, "conditions", conditions)
        if supported_features is not None:
            pulumi.set(__self__, "supported_features", supported_features)

    @_builtins.property
    @pulumi.getter
    def conditions(self) -> Optional[Sequence['outputs.XMeshStatusConditionsPatch']]:
        """
        Conditions is the current status from the controller for
        this Mesh.

        Controllers should prefer to publish conditions using values
        of MeshConditionType for the type of each Condition.
        """
        return pulumi.get(self, "conditions")

    @_builtins.property
    @pulumi.getter(name="supportedFeatures")
    def supported_features(self) -> Optional[Sequence['outputs.XMeshStatusSupportedFeaturesPatch']]:
        """
        SupportedFeatures is the set of features the Mesh support.
        It MUST be sorted in ascending alphabetical order by the Name key.
        """
        return pulumi.get(self, "supported_features")


@pulumi.output_type
class XMeshStatusSupportedFeatures(dict):
    def __init__(__self__, *,
                 name: Optional[_builtins.str] = None):
        """
        :param _builtins.str name: FeatureName is used to describe distinct features that are covered by
               conformance tests.
        """
        if name is not None:
            pulumi.set(__self__, "name", name)

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        FeatureName is used to describe distinct features that are covered by
        conformance tests.
        """
        return pulumi.get(self, "name")


@pulumi.output_type
class XMeshStatusSupportedFeaturesPatch(dict):
    def __init__(__self__, *,
                 name: Optional[_builtins.str] = None):
        """
        :param _builtins.str name: FeatureName is used to describe distinct features that are covered by
               conformance tests.
        """
        if name is not None:
            pulumi.set(__self__, "name", name)

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[_builtins.str]:
        """
        FeatureName is used to describe distinct features that are covered by
        conformance tests.
        """
        return pulumi.get(self, "name")


